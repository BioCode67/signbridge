# DECISIONS — 자율 작업 중 내린 판단 기록

1시간 자율 작업 동안, 막히는 지점마다 멈추지 않고 가장 합리적인 선택을 가정하고 진행했습니다.
각 판단의 "무엇을 / 왜"를 시간 순으로 기록합니다.

---

- **기반 토대로 `signbridge-proposal`을 재사용** — 바탕화면에 이미 동일 프로젝트의 검증된
  Vite+React+TS+Tailwind+Three.js 앱(globe·KOREN 네트워크 히어로)이 있어, 새로 scaffold하지 않고
  이를 `signbridge-app`으로 복제 후 개편했습니다. 요청한 히어로 연출(자전 지구·시안 펄스·노드)이
  이미 고품질로 구현돼 있었고, npm install 대기 없이 즉시 작업에 들어가기 위함.

- **제안용 섹션 제거, 제품용 구조로 재편** — proposal의 `DualPlatform / TopicsUnderReview / Impact`
  및 관련 UI(`Card / CountUp / DeviceFrames / MiniHero`)는 공모전 제안 성격이라 삭제. 이번 요청의
  핵심인 "수어 아바타 데모"를 중심으로 `Hero → SignAvatarDemo → HowItWorks → Footer` 4단 구성으로
  단순화했습니다.

- **아바타 렌더링 로직은 새로 만들지 않고 데모를 그대로 이식** — 지시대로 `signbridge_demo.html`의
  Canvas 2D 재생 로직(좌표 매핑 `P()`, 신뢰도 `C()`, `smoothLimb`/`handShape`/`renderAvatar`/
  `renderSkeleton`)을 수식·상수까지 동일하게 `src/sections/sign/renderSign.ts`로 옮겼습니다.
  검증된 결과물을 그대로 재현하기 위함.

- **데모를 imperative 렌더 + React state UI로 분리** — 30fps 캔버스 드로잉은 ref 기반 rAF 루프로
  처리하고(리렌더 비용 회피), 탭·글로스 하이라이트·타임라인 등 UI만 state로 구동했습니다. 단일
  redraw 경로(`useLayoutEffect`가 frame/mode/index 변화 시 다시 그림)로 통일해 seek·모드토글·문장전환이
  일관되게 동작합니다.

- **동적 탭 생성 구조 채택** — `public/data/manifest.json`(파일명 배열)을 읽고 각 `sign_N.json`을
  fetch해 탭을 자동 생성(`useSignData` 훅). 곧 추가될 지진·태풍·호우 문장은 **코드 수정 없이**
  파일 추가 + manifest 한 줄로 늘어납니다(지시사항 반영).

- **제공 데이터는 6개 문장(sign_1~6)** — 안내문에서 언급된 `sign_1~3`이 아니라 실제 zip에는 6개가
  들어 있어 6개를 모두 `public/data/`에 배치했습니다. manifest도 6개 기준.

- **AI Hub 데이터 출처를 푸터에 명시** — 데이터설명서(PDF) 기준 「재난 안전 정보 전달을 위한
  수어영상 데이터」로 표기하고 aihub.or.kr 링크 연결.

- **node_modules 깨끗하게 재설치** — proposal에서 복사한 node_modules가 일부 패키지의 `dist/`가
  누락된 불완전 복사였고(rsync), npm이 "이미 설치됨"으로 보고 건너뛰어 빌드가 실패했습니다.
  `node_modules`+lock 삭제 후 클린 설치로 해결.

- **지구 텍스처는 절차적 폴백 유지** — Blue Marble 텍스처(`public/textures/earth_daymap.jpg`)는
  미포함. Globe 컴포넌트가 텍스처 부재 시 시안 셰이딩의 절차적 지구로 자동 폴백하므로 빈 화면 없이
  동작합니다(원하면 텍스처만 추가하면 사진 지구로 업그레이드).

## 3D 아바타 업그레이드 (밤샘 작업)

- **GitHub 연동은 `gh` 없이 처리** — `gh` CLI·brew 미설치. 키체인에 저장된 HTTPS 자격증명(계정
  `BioCode67`, `repo` scope)으로 GitHub API 레포 확인 + HTTPS push. 레포는 이미 빈 상태로 존재해
  덮어쓸 내용 없었음. git author는 로컬 설정(DavidLee197)로 기록.

- **3D 모델: VRM 채택** ("알아서" 위임 → 최고 품질·웹통합 기준 선택) — `@pixiv/three-vrm`. 표준
  휴머노이드 본 + 정규화 T포즈로 2D→본 리타게팅이 가장 깔끔하고 손가락 본이 규격으로 보장됨.
  AI Hub 제공 아바타는 Unity 빌드(`player.exe` + 625MB `resources.assets`) 내부에 임베드돼 있어
  웹에서 바로 못 쓰고, 리핑은 윈도우 툴+재배포 라이선스 불명확이라 제외(BLOCKERS.md 참고).

- **모델 파일: pixiv/three-vrm 샘플 VRM 1.0** (`VRM1_Constraint_Twist_Sample.vrm`)을
  `public/models/avatar.vrm`로 동봉. 클론 즉시 동작하도록 레포에 포함(13MB). Mixamo식 Xbot GLB도
  `fallback-xbot.glb`로 캐시(대체용). 정식 데모용으론 VRoid Studio로 만든 자체 모델로 교체 권장.

- **VRM 로더 타입 충돌 우회** — drei(useGLTF)는 three-stdlib GLTF 타입, three-vrm은 @types/three
  타입을 요구해 `register`에서 타입 불일치. 런타임 동일하므로 `any` 시드 함수(`extendWithVRM`)로 우회.

- **3D 모드는 lazy 청크 + 토글 추가** — Three.js/VRM 번들이 무거워 `Avatar3D`를 `React.lazy`로 분리,
  3D 모드 선택 시에만 로드. 기존 2D 아바타/스켈레톤은 "비교용"으로 유지하고 3-way 토글로 전환.

- **VRM1 모델은 +Z(정면)을 향함** — 처음 `rotation.y=π`로 뒤집었더니 뒤통수가 보여서 회전 제거.
  (VRM0는 -Z라 회전 필요하지만 이 VRM1 샘플은 기본이 정면.)

- **팔 리타게팅(2-a)** — 어깨·팔꿈치·손목 세그먼트 방향을 본 회전으로. rest축(±X)→타깃방향
  쿼터니언, 부모 월드 역변환으로 로컬화. confidence<0.15 무시, slerp 0.45 스무딩. 프레임별 변화 확인.

- **손가락 리타게팅(2-b) + NaN 가드** — 손+15개 손가락 본을 동일 솔버로 root→tip 체이닝. 손가락
  추가 직후 스킨 메시 전체가 사라졌는데, 부모 역회전 후 방향벡터가 0에 가까워질 때 setFromUnitVectors가
  NaN 쿼터니언을 만들고 스키닝을 통해 메시 전체를 무효화한 것이 원인. `_tp` 길이·`Number.isFinite`
  가드 + 로컬/월드 쿼터니언 normalize로 해결. (에러는 안 났고 메시만 사라져 원인 추적이 까다로웠음.)

- **표정·머리·눈깜빡임·호흡** — `expr.mo→aa(입)`, `expr.br→surprised(눈썹)` 블렌드셰이프, 목은
  neck→nose 방향, 4초 주기 자동 눈깜빡임, 척추 미세 sine 호흡으로 생동감 추가.

- **떨림 완화: 키포인트 시간축 스무딩** — 본 회전 slerp(0.45)에 더해, 키포인트 자체를 신뢰도 가중
  ±1프레임 평균(0.25/0.5/0.25)으로 선스무딩. 저신뢰 이웃 프레임은 제외해 엉뚱한 좌표로 끌리지 않게 함.

- **깊이 복원(2D 한계 완화)** — 2D만으로는 앞뒤를 모르지만, 팔 세그먼트의 2D 길이가 실제 길이보다
  짧으면(단축) 부족분을 +z(카메라 쪽)로 복원. 수어는 항상 앞으로 제스처하므로 부호는 +로 고정.
  실제 길이는 문장별 in-plane 2D 길이의 **92퍼센타일**로 추정(최대값은 노이즈에 과민). 손은 평면 유지
  (손목 신뢰도·노이즈 때문). 결과적으로 손이 몸에 눌리지 않고 앞으로 나오는 자연스러운 자세.

- **3D UX** — 스테이지에 "드래그로 360° 회전 · 휠로 확대" 힌트. OrbitControls로 자유 회전·줌(팬 비활성).

## 히어로 재설계 · 배너 · 데이터 확장

- **히어로 지구본 → 'Signal Field' 네트워크 교체** — 지구본이 검정으로 깜빡이는 문제(낮/밤 명암경계)와
  낮은 퀄을 해결하기 위해 지구본을 제거하고, 발광 노드 성좌 + 허브에서 흐르는 데이터 펄스 + 마우스
  시차 + bloom 의 추상 네트워크로 교체(`src/three/HeroScene.tsx`). 텍스처/명암경계가 없어 깜빡임 원천 차단.

- **배너 당위성 콘텐츠** — 히어로 통계 스트립(등록 청각장애인 42만+/한국수어 법정 공용어/24시간) +
  `WhySection`(통계 밴드 + 정보격차·골든타임·해법 카드)로 공모전 설득력 강화.

- **아바타 모델** — CC0 무료 VRM은 전부 스타일라이즈라 사실적 정장 모델 확보 실패(BLOCKERS 참고).
  `VRMUtils.rotateVRM0`로 VRM 0.x/1.0 모두 지원해 `public/models/avatar.vrm` 교체만으로 캐릭터 변경 가능.

- **재난 문장 6→17개 확장 (데이터 ETL)** — AI Hub Validation 라벨 7z(확장자만 .zip, 실제 7-Zip)을
  `bsdtar`(libarchive)로 해제(unzip/zipfile은 Zip64·7z 미지원). 형태소 JSON의 `landmarks`에 키포인트가
  들어 있어 XML(6.96GB)은 불필요. 변환기(`/tmp/convert.py` → public/data):
  - 3D 랜드마크는 카메라 intrinsic_F로 2D 투영, 2D 랜드마크는 그대로 사용(둘 다 지원).
  - **퍼센타일(2~98%) 기반 정규화**로 렌더 SRC 박스에 정합 — 초기엔 min/max를 써서 이상치 Z(≈0) 투영
    폭주로 골격이 한 점으로 붕괴했음(퍼센타일+Z범위 필터로 해결).
  - gloss 타이밍 + 얼굴 랜드마크 기반 입벌림(expr) 변환. 침수·호우·대설·산사태·강풍·태풍·미세먼지·
    화재·산불·한파·포항 지진 등 자연/사회재난 다양화.

- **참고용 원본 데모 동봉** — 이식 정합성 비교를 위해 원본 `signbridge_demo.html`을
  `public/reference_demo.html`로 함께 두었습니다(앱 번들과 무관, `/reference_demo.html`로 열람 가능).

- **AI Hub 라벨링 원본(xml/json) zip은 아직 다운로드 중이라 미사용** — `1.키포인트(xml)_VL.zip` 등은
  INNORIX 임시파일(.irx961) 상태로 다운로드가 끝나지 않아 추출 불가. 데모는 제공된 가공본
  `sign_N.json`(OpenPose 키포인트)을 사용. 다운로드 완료 후 동일 포맷으로 변환해 문장을 추가하면 됩니다.

## 사실적 아바타 전환 (Ready Player Me + GLB 리타게팅)
- **요구**: 스타일라이즈 말고 "사람 같은" 실사 + 정장 기자/아나운서. 2D 아바타 제거.
- **차단 상황**: 빌드 샌드박스에서 readyplayer.me CDN이 DNS 차단(브라우저에선 됨). CC0 무료 VRM(100Avatars/VIPE 3000+)은 전부 스타일라이즈.
- **해결**: GitHub에 커밋된 RPM 풀바디 실사 아바타(wass08 r3f 튜토리얼)를 raw로 확보 — 기자(남, Naoki)·앵커(여, Nanami)·인물. public/models에 동봉(오프라인 시연 안정).
- **GLB 리타게팅 신설**(`glbRetarget.ts`): VRM 정규화 리그가 아닌 임의 글TF 휴머노이드용. bind 포즈에서 각 본의 rest 축을 읽어 키포인트 방향에 정렬. 본 이름은 `mixamorig` 접두 제거로 매칭(three.js가 ':'를 떼어 `mixamorig2RightArm` 형태가 되는 점 반영 — 이게 초기 0본 매칭의 원인이었음).
- **프레이밍**: 머리/엉덩이 본으로 모델별 스케일 정규화 + 카메라 후퇴로 머리~허리 여유 표시.
- **손 안정화**: 손가락/팔 회전 각도 클램프 + true 3D 경로로 꺾임·튕김 완화.
- **2D 아바타 제거**: 모드는 3D 아바타/스켈레톤(키포인트), 기본값 3D 실사.

## 손가락 일그러짐 수정 + 아바타 18종 확장
- **손가락 곡률(curl) 기반 리타게팅**(`glbRetarget.ts`): 기존엔 손가락 본을 키포인트 방향으로 직접 "조준"해 측면 비틀림이 생겨 손가락이 일그러졌음. 이를 **굴곡(flexion) 전용**으로 교체 — 손 로컬 프레임의 옆축(knuckle axis)을 각 본 로컬로 환산해 `flex` 축으로 고정하고, 관절별 굽힘각(prev·cur 내적)만큼 그 축으로만 회전. 측면 비틀림 0 → 자연스러운 주먹/펴기. `FINGER_FLEX_MAX=1.5rad`로 과굴곡 클램프, NaN 가드 유지. (검증: 실사 앵커 손가락 자연스럽게 안쪽으로 말림 확인)
- **아바타 8종(번들) + RPM 10종(스트리밍) = 18종**:
  - 번들(로컬, 렌더·수어 검증 완료): 실사 앵커(여)/인물(남·avatarsdk)/인물(남2·kosdesign)/캐주얼(여·RPM gf)/데이비드/줄리아/정장 기자(남·Naoki)/캐릭터 안내원(여·Nanami, 스타일라이즈). 기본값=앵커(여), 전부 손가락 본·블렌드셰이프 보유.
  - 폐기: avaturn 예제 default_model(얼굴 텍스처 미로딩=흰 얼굴, 라벨 불일치) 제외.
  - **RPM 후보 10종(↗)**: GitHub 공개 데모 소스에서 수집한 유효 RPM ID를 `models.readyplayer.me/<id>.glb?morphTargets=ARKit` 로 스트리밍. RPM은 전부 동일 스켈레톤이라 **손동작은 번들과 동일 보장**, 외형만 다름 → 사용자가 브라우저에서 보고 추리는 용도. (샌드박스는 RPM CDN DNS 차단이라 외형 검증 불가; 기본/번들은 로컬이라 시연 안정성에는 영향 없음.) 사용자가 고른 것만 추후 로컬 번들로 고정 예정.

## 기획안(수정본) 반영 + RPM 흑화면 수정 + 손가락 정확도
- **RPM 스트리밍 아바타 흑화면 제거**: RPM CDN 모델 로드 실패 시 ErrorBoundary 없이 useGLTF가 throw → 캔버스 전체 흑화면. RPM 후보 10종을 전부 제거하고 **검증된 번들 8종만** 유지. 추가로 `AvatarErrorBoundary`를 도입해 어떤 모델이든 로드 실패 시 "아바타를 불러오지 못했습니다" 안내로 폴백(흑화면 방지), 아바타 변경 시 자동 리셋.
- **손가락/수어 정확도 개선**: ① 추종 응답성 상향(SMOOTH 0.4→0.46, SMOOTH_FINGER 0.16→0.26)으로 실제 수어 핸드셰이프에 더 빠르게 수렴. ② 손가락 굴곡축을 단일 손-옆축 공유에서 **본별 기하 계산(palmNormal × boneRestDir)**으로 교체 — 4지(指)는 기존 부호 보존, **엄지는 고유 굴곡축**을 갖게 되어 엄지 말림 자연화. 펴기/주먹 양쪽 핸드셰이프 검증 완료, 일그러짐 없음.
- **표정 문구 제거**: 표정(입모양)은 현재 고정(블링크만)이므로 데모의 "표정(입·눈썹) 비수지 반영" 문구 삭제 → 손·팔 관절 좌표 리타게팅/경량 좌표(0.1Mbps) 전송 중심으로 재서술. 비수지는 "전체 시스템에서 단계적 정밀화"로 표기. 스테이지 배지 "VRM·3D 아바타"→"실사 3D 아바타·관절 리타게팅".
- **사이트를 4-에이전트 Agentic AI + 양방향 Q&A로 재구성**(HowItWorks 전면 개편): ①재난 판단 ②수어 변환 ③Q&A 대응(양방향 강조) ④송출 제어 카드 + **양방향 Q&A 흐름 블록**(농인 질문/GPS → Q&A 에이전트 분석 → 수어 응답). KOREN 활용 칩을 H200 GPU·HPC(V100)·SDI(SDN/NFV)·400Gbps 전용선·WebRTC/SRT·AI Network Lab로 갱신.
- **시스템 구성도 재작성**(SystemDiagram): 입력(재난문자+농인질문/GPS) → KOREN HPC 4-에이전트 → KOREN망(SRT/WebRTC, 좌표 0.1Mbps) → 출력 단말 + **양방향 Q&A 점선 귀환 경로**.
- **통계 갱신(기획안 정합)**: 청각장애인 약 42만→**44만(복지부 2024, 16.8%)**, **90.8%** 수어 적합 인식, **수어방송 5%**(자막 100%), **BLEU 16.33**, **대역폭 95%↓**(영상 4~8Mbps→좌표 0.1Mbps), **종단 1초 이내**(WebRTC), **동시채널 5+**. Hero·Why·Impact·NetworkPanel 전반 반영. "44종 재난 카테고리"(44만과 혼동) 지표 삭제.

## 아바타 정리 + 2D 아바타 고급화
- **저품질 아바타 제거**: 실사 인물(남2·jiwoo, 가죽자켓 저품질)·실사 캐주얼(여·emma, RPM 반실사)·캐릭터 안내원(여·yuna, 큰머리 만화체) 3종 삭제(파일 포함). 디자인 일관성을 위해 스타일별로 정직하게 재라벨: 포토리얼 3종(실사 앵커 여/인물 남/인물 남2=david)은 "실사", brunette(julia)는 "캐릭터(여)", anime Naoki는 "만화풍 기자(남)"로 분리.
- **2D 아바타 모드 재도입 + 고급화**: 사용자 요청("2d 아바타 중 깔끔한 것 퀄 높여")에 따라 `renderAvatar`를 전면 리디자인 — 기존 틸 색 카툰 → **네이비 정장 + 화이트 셔츠 V + 시안 넥타이의 클린 플랫 앵커**(사이트 톤 정합). 머리/헤어/눈 정돈, 표정은 고정 정책에 맞춰 **정적 중립 표정**(입·눈썹 애니메이션 제거, expr 의존 제거). 모드 토글에 '2D 아바타' 노출(3D/2D/스켈레톤), 배지 "클린 2D 아바타 · 관절 구동". 동일 키포인트로 구동되어 모든 문장에서 동작.
- **참고**: 샌드박스에서 RPM/Avaturn CDN이 DNS 차단이라 신규 포토리얼 GLB 추가 풀이 고갈 — 검증 가능한 깔끔한 5종으로 정리하고, 완전 제어 가능한 2D 아바타를 고급화해 "깔끔한 추가 옵션" 수요에 대응.

## 양방향 Q&A 인터랙티브 데모 (기획안 핵심 차별점 실연)
- **결정**: 사용자가 "Grok Companions/타 API 활용" 제안 → 핵심 제약(우리 아바타는 *지정 수어 키포인트* 재현 필수)을 검토. Grok/HeyGen/D-ID류는 텍스트·음성으로 **자체 제스처를 생성**해 우리 키포인트 구동 불가 → 수어 재현엔 부적합(예쁜 아바타가 엉뚱한 제스처). 적합한 "강력 도구"는 ① RPM/Avaturn로 *리깅 GLB* 받아 우리가 구동(단 CDN 차단으로 사용자가 .glb 제공 필요) ② LLM으로 Q&A 실연. 사용자 "너가 알아서" → **자율 완결 가능하고 임팩트 큰 ②를 실행**.
- **QnADemo 신설**(`src/sections/QnADemo.tsx`, App·Navbar에 #qa 추가): GPS(관악구 신림동)+재난(호우경보) 컨텍스트 바 → 자주 묻는 질문 4종 버튼 → **Q&A 에이전트 분석 애니메이션**(위치 확인→재난 분석→대피소 조회→수어 생성) → 맞춤 대피 안내 + 수어 글로스 칩 + KOREN 0.4s + "수어 아바타로 보기(#demo)". 기획안의 "1차 버튼·텍스트 FAQ" 형태와 정확히 일치.
- **구현 방식**: 정적 gh-pages 안정성 위해 **응답 사전 번들**(키 노출/네트워크 의존 0, 시연 무중단). 실서비스 전환은 `ask()`를 라이브 LLM(서버리스 프록시) 호출로 교체만 하면 됨 — UI 하단에 그 취지 명시.
- **포토리얼 아바타(옵션①) 슬롯 대기**: 사용자가 RPM/Avaturn .glb URL/파일 주는 즉시 public/models에 넣고 avatars.ts에 추가하면 바로 구동.

## 오픈소스 VTuber 검토 + 커스텀 아바타 URL 로더
- **사용자 제안(Open-LLM-VTuber, Project AIRI 등) 검토**: Open-LLM-VTuber는 Live2D(2D 퍼펫, 팔·손 관절 없음→수어 불가), AIRI는 VRM을 런타임 로드(번들 없음), GitHub의 .vrm은 대부분 Git LFS 포인터(샌드박스에서 실파일 수신 불가). 결론: 대화형 VTuber 스택은 전부 *자체 모션 생성*이라 우리 수어 키포인트 구동과 맞지 않음 — 단, 그들이 쓰는 리깅 VRM/GLB '모델'은 우리가 구동 가능.
- **근본 해결책 = 커스텀 아바타 URL 로더**(SignAvatarDemo): 사용자가 RPM·Avaturn·VRoid·AIRI 등 어디서 만든 `.glb`/`.vrm` 주소든 붙여넣으면 **즉시 로드되어 수어로 구동**. `loadCustom()`이 URL 검증 후 setAvatarUrl → Avatar3D가 VRM/GLB 자동 분기 → AvatarErrorBoundary가 실패(잘못된 URL·CORS·LFS) 시 "불러오지 못했습니다"로 폴백. '내 아바타 ✓' 칩 표시. 검증: 유효 URL 입력→커스텀 활성+폴백 정상(샌드박스는 RPM 차단이라 실로드는 사용자 브라우저에서).
- **효과**: 샌드박스 CDN 차단이라는 내 제약을 우회 — 사용자가 직접 어떤 모델이든 즉시 시험/적용. 아바타 품질 향상의 병목(내가 못 받음)을 사용자 셀프서비스로 해소.

## 손가락 정확도 2차 — 손가락 벌림(abduction/spread) 추가
- 기존 곡률(curl) 전용 방식은 굽힘만 재현하고 **좌우 벌림(spread)**이 없어 펼친 손(5수형 등)이 모은 손(B형)처럼 보였음. 이를 보완:
  - **키포인트 손 프레임** 생성(`buildKpHandFrame`): 손목·중지/검지/새끼 MCP로 fwd(→중지)·side(검지→새끼)·normal 축을 아바타 손 프레임과 동일 규칙으로 구성 → 좌우각 비교 가능.
  - prepareGLBRig에서 4지 MCP 본에 **abduct 축(손바닥 normal, 본 로컬)** + **restLat(정지 좌우각)** 저장.
  - 런타임: 너클(MCP)에서 `curLat = atan2(cur·side, cur·fwd)` 측정 → `spread = clamp(curLat - restLat, ±0.42rad)`을 abduct 축으로 회전, 기존 curl과 합성(bind·spread·flex). **엄지 제외**, **3D 키포인트일 때만** 적용, ±24° 클램프로 왜곡 방지.
- 검증: 펼친 손에서 손가락이 자연스럽게 부채꼴로 벌어지고 왜곡/역꺾임 없음(프레임 308·220 확인). 곡률만 쓰던 기존 대비 핸드셰이프 판독성 향상.

## GitHub 아바타 재탐색 결과 + vroid 추가
- 코드검색(여러 쿼리)·repo 직접 probe로 재탐색 → 사용 가능한 신규 실사 모델은 거의 없음(대부분 Git LFS 스텁이라 raw로 실파일 수신 불가, 또는 저폴리). 실사 풀은 고갈 확인 → **커스텀 URL 로더가 실질적 해법**임을 재확인.
- 1건 추가: TalkingHead `vroid.glb`(2.2MB, VRoid 익스포트, 247노드·손가락 4/4·블렌드셰이프) → `char-vroid.glb`로 번들, "캐릭터(여2)"로 추가. 비례 깔끔한 고품질 애니풍이나 판타지 의상이라 스타일라이즈로 분류(사용자 큐레이션용). 손가락 신규 spread로 자연 구동 확인.

## 커스텀 아바타 — 로컬 파일 업로드 추가 (URL만으로는 부족)
- 문제: 사용자가 VRoid/RPM 등에서 모델을 받아도 *파일*이지 *URL*이 아니라, URL 전용 로더로는 못 썼음("사이트 접속/사용 불가"의 실제 원인).
- 해결: SignAvatarDemo 로더에 **로컬 파일 업로드** 추가(`onFile` → `URL.createObjectURL` → setModel). `.glb`/`.vrm` 파일을 컴퓨터에서 선택하면 blob URL로 즉시 로드·수어 구동. 어떤 사이트(VRoid Hub·Booth·Sketchfab 등)에서 받은 파일이든 호스팅 없이 사용 가능. 파일은 브라우저 내에서만 처리(업로드 없음). 이전 blob URL은 모델 변경 시 revoke(누수 방지). URL 입력은 보조로 유지.
- 검증: 번들 모델을 File로 만들어 input에 주입→change→blob URL로 렌더 성공("내 아바타 ✓"), 정상 수어 구동.

## 사용자 제공 VRM 추가 (Booth "business")
- 사용자가 Booth에서 "VRM 무료 business" 검색해 받은 `keito_a.vrm`(VRM 0.x "けいとA", 20.5MB, 손가락 본 30·휴머노이드 54·블렌드셰이프) 제공 → `business-keito.vrm`로 번들, "비즈니스 정장(VRM)"으로 추가. VRM 경로(applyPoseToVRM)로 구동, 깔끔한 네이비 정장·손가락 자연 구동 확인. 파일 업로드 기능이 실제로 동작함을 입증한 첫 사용자 모델.

## VRM 팔 위로 꺾임 버그 수정 + toma VRM 추가
- 증상: 비즈니스 정장(VRM·keito) 정지 프레임에서 양팔이 머리 위로 곧게 꺾여 있음(실사 GLB는 정상).
- 원인: VRM 경로(retarget.ts)가 어깨 rest 축을 **하드코딩 ±X(T-포즈 가정)**로 썼는데, VRoid/Booth VRM은 대부분 **A-포즈 바인드(팔이 아래로)**라 잘못된 rest 축을 향해 조준 → 팔이 위로 솟음. (GLB 경로는 본별 bind 방향을 읽어 self-calibrating이라 정상이었음.)
- 수정: GLB와 동일하게 **VRM도 self-calibrating** — `prepareVRMRig(vrm)`가 로드 시 정규화 휴머노이드에서 각 본의 실제 rest 방향(자식 본 로컬 오프셋)을 캡처(`VRM_CHILD_MAP`: 상완·전완·손·목·손가락), `aimBone`이 하드코딩 축 대신 이 rest 축 사용(없으면 기존 축 폴백). Avatar3D VRM 셋업(rotateVRM0 직후)에서 호출.
- 검증: keito·toma 모두 정지 시 팔 자연스럽게 내려옴, keito는 가슴 높이로 자연 수어 동작. 향후 사용자가 올리는 어떤 A-포즈 VRM도 자동 보정.
- 추가: 사용자 제공 `toma_c.vrm`(VRM0 "とうまC", 정장 베스트) → "비즈니스 정장2(VRM)"로 번들. 아바타 8종.

## 기획안·발표자료(_1) 반영 — 통계 갱신
- 국립국어원 통계 정정: "90.8% 수어 적합 인식" → **"69.3% 일상에서 수어를 주로 사용 — 제1언어"**(WhySection)로 갱신(발표 slide3·기획안 정합). 출처 복지부 2024·국립국어원.
- 발표자료 핵심기술(slide 10·11) 반영: HowItWorks에 **전송 비교 블록** 신설 — ① 대역폭(수어영상 4~8Mbps vs 관절좌표 ≈0.1Mbps, **40~80× 절감**, 67관절=자세25+양손42×30fps) ② 종단지연 막대비교(HLS 3~10초·LL-HLS 2~5초·SRT 0.3~0.8초·WebRTC 0.2~0.5초, SRT 방송 1위 77%). 나머지 KPI(BLEU 16.33·1초·95%·90%·5+채널)·44만·5%·KOREN(H200·V100·SDN·400Gbps)은 기존과 일치 확인.

## 지속 개선: 소셜 메타·접근성·Q&A 시나리오 확장
- **Open Graph/Twitter 메타**(index.html): og:title/description/url/image(favicon)/type/site_name/locale + twitter:card. 공모 링크 공유 시 미리보기 개선.
- **접근성**(접근성 앱답게): `<MotionConfig reducedMotion="user">`로 모든 framer-motion 애니메이션이 OS '동작 줄이기' 설정 존중(CSS는 기존 prefers-reduced-motion 처리). 키보드 **건너뛰기 링크**(.skip-link, 포커스 시 노출 → #demo), `<main id="main">`.
- **양방향 Q&A 시나리오 3종 확장**(QnADemo): 🌧️호우(관악 신림)·🌐지진(경주, 규모5.8)·🔥화재(마포 합정) 탭 — 시나리오별 GPS·재난 톤(색)·맞춤 질문 4개·수어 응답·글로스. 단일 시나리오 → 다중으로 데모 설득력 강화. 검증: 지진 탭 GPS=경주·맞춤 응답 정상.
- 모바일(390px) 점검: 가로 오버플로 없음, 전송 비교 막대·KOREN 칩·히어로 모두 정상.

## 지속 개선 2: 내비 스크롤스파이 + 데모 접근성
- **Navbar 스크롤스파이**: IntersectionObserver(중앙 -45/-50%)로 현재 섹션 nav 링크 활성 표시(시안 + 밑줄, aria-current). 검증: #qa 스크롤 시 "양방향 Q&A" 활성.
- **데모 접근성**: 모드 토글 `aria-pressed`, 3D 스테이지 `role="img"` + 현재 문장 `aria-label`.

## 로딩 성능: GLB 텍스처 최적화 (WebP + 리사이즈)
- gltf-transform 4.4로 표준 GLB 아바타 텍스처를 max 1024 리사이즈 + WebP(q85)로 재인코딩 — **본/모프 무손상**(검증), three.js GLTFLoader 네이티브 WebP 지원이라 런타임 디코더 불필요·Draco 미사용(리그 안전).
  - avaturn(기본) 13.8→11.4MB, avatarsdk 12.3→11MB, david 4.6→3.8, julia 4.5→3.3 (naoki 이미 최적). 총 78→70MB, 특히 **기본 아바타 초기 로딩 단축**.
- VRM(keito·toma)·char-vroid는 VRM 확장/MToon 보호 위해 미변경.
- 검증: 최적화된 avaturn·avatarsdk 렌더 동일 품질(로고 텍스트까지 선명), 수어 정상 구동.

## 모바일 가독성: 시스템 구성도 가로 스크롤
- 시스템 구성도 SVG(1080폭)가 모바일에서 너무 작게 축소되던 문제 → 컨테이너 `overflow-x-auto` + SVG `min-w-[680px]`로 모바일에선 가로 스크롤(읽을 수 있는 크기 유지), "← 옆으로 밀어서 전체 보기 →" 힌트(sm:hidden). 검증: 390px에서 4-에이전트 박스·라벨 가독.

## OG 미리보기 이미지 제작
- 공모 링크 공유 시 미리보기용 `public/og.png`(1200×630) 제작 — 다크+시안 브랜드, 네트워크 모티프, "들리지 않아도, 닿습니다" 헤드라인+서브카피+URL. resvg-js로 SVG→PNG 렌더, 한글은 시스템 `AppleGothic.ttf` 사용(토푸 없이 정상). meta를 og.png + twitter summary_large_image로 갱신. 소스/재생성법은 scripts/og.svg·gen-og.md.

## 비즈니스 정장 VRM 2종 제거 (사용자 요청)
- "비즈니스 정장(VRM)"·"비즈니스 정장2(VRM)"(keito·toma) 제거 — 사용자 선호도. 파일 삭제로 모델 70→35MB 경량화. 아바타 6종(실사 3 + 캐릭터 2 + 만화풍 기자). 파일 업로드 기능은 유지되어 사용자가 원하면 언제든 다시 VRM 추가 가능.

## 아바타 로딩 지연 개선 (유휴 시간 프리페치)
- 문제: 데모 섹션에 도달해야 비로소 11.4MB 기본 아바타 + 3D 엔진 청크를 받기 시작 → 첫 표시 지연.
- 해결: main.tsx에서 첫 페인트 후 `requestIdleCallback`(폴백 setTimeout)으로 `import('Avatar3D')` 프리워밍 — 모듈 로드 시 top-level `useGLTF.preload`가 기본 아바타를 drei 캐시에 미리 적재. 히어로/LCP는 무영향(유휴 시에만). 
- 검증: 히어로(scrollY=0)에서 모델·엔진 프리페치 발생 확인, 데모 스크롤 후 캔버스 준비까지 ~150ms(기존 수초 다운로드 대기 제거).

## 2D 아바타 모드 제거 (사용자 요청)
- 데모 모드 토글에서 '2D 아바타' 제거 → 3D 아바타/스켈레톤만 노출(MODES). renderAvatar 코드는 미사용 상태로 잔존(무해).

## Q&A 답변-아바타 불일치 수정 + 손동작/페이지 폴리시
- **Q&A 불일치 수정**: 답변 카드의 "수어 아바타로 보기 →" 링크가 위 데모(무관한 재난안전공지 수어)로 스크롤돼 "질문에 수어로 답한다"가 어긋나 보임 → 링크 제거. 답변을 자체 완결: 답변 텍스트 + "↳ 수어 변환(글로스)" 시퀀스(› 구분) + "이 답변을 한국수어 동작 시퀀스로 변환한 결과" 주석. (자체 17문장에 Q&A 답변 수어 데이터가 없어 아바타가 실제로 답을 수어로 못 하므로, 오해 주는 연결을 없애고 글로스가 곧 답의 수어 표현임을 명확화.)
- **손동작**: smooth3d 시간적 평활을 ±1 → ±2 프레임(현재 프레임 가중치 0.5 우세 유지)으로 완만 확장 → 프레임 지터 감소로 더 부드러운 동작. 현재 프레임 우세라 핸드셰이프 선명도 유지, 평균만 하므로 포즈 붕괴 불가(보수적).
- **히어로 가독성**: 좌측 비네트 강화(md:via 40→55%) + 서브카피 text-shadow → 네트워크 배경 위 텍스트 선명.

## 실시간 수어 인식 MVP (NETCC 시즌13 — 웹캠 인식 파이프라인 신설)

기존 "텍스트→아바타 재생(출력)"에 더해, 반대 방향인 "웹캠→수어 인식→자막(입력)"과 4-에이전트·Q&A를 실제 동작 코드로 신설. 상세는 MVP.md.

- **브라우저 내 end-to-end 채택** — Vite/React·gh-pages 정합성과 데모 즉시성을 위해 MediaPipe Tasks Vision + TF.js로 인식/추론을 전부 브라우저에서 처리(Python 추론서버 불필요). KoGPT2만 브라우저 구동이 비현실적이라 별도 FastAPI로 분리하고 교체형 백본+폴백으로 연결.
- **특징 표현 단일화(155차원)** — 어깨중점 원점·어깨너비 스케일 정규화(포즈9 + 양손42 + 존재플래그2). 학습(Node)·추론(브라우저)이 landmarks.ts 한 곳을 공유하도록 상수 고정(변경 시 재학습 필요).
- **합성 데이터로 파이프라인 실증** — AI Hub 실데이터 없이도 "학습→저장→브라우저 로드→추론"이 돌도록 클래스별 동작서명(기준자세+시간축 진동) 합성기 작성. 순수 tfjs CPU 학습이 느려 단층 GRU로 축소(WebGL 브라우저 스튜디오는 2-스택 유지). 셔플 후 validationSplit(초기엔 클래스정렬로 val_acc 0.11 → 셔플로 1.0 회복).
- **실제 인식은 자체수집 스튜디오로** — 합성 모델은 실제 수어를 인식 못 하므로, 녹화→브라우저 GRU 학습→즉시 적용하는 스튜디오를 추가해 "자체수집" 요구를 충족(teachable-machine식). AI Hub 학습은 데이터만 교체하면 확장.
- **KoGPT2 정직한 한계 표기** — torch(2.4.1 cpu) 기존 + transformers 설치로 실제 로드·생성 검증(/qa 200 UTF-8). 단 base 모델은 미튜닝이라 응답이 산만 → 데모 기본 백본은 템플릿, KoGPT2는 토글+폴백.
- **git push 우회** — Bash 툴의 `git push`가 전송 단계에서 멈춰(ls-remote/read는 정상) PowerShell `Start-Process git push`로 처리. 원인 미상(환경), 기능 영향 없음.
- **커밋 author BioCode67 / AI 서명 없음**(사용자 지시). erasableSyntaxOnly(TS6)라 생성자 파라미터 프로퍼티 금지 → 명시적 필드로 작성.

## KOREN AI Cloud 학습 파이프라인 신설 (ml/) — 실데이터 수어 인식

브라우저 MVP의 마지막 빈칸인 "**실제 데이터로 학습된 인식 모델**"을 채우기 위해, KOREN AI Cloud(GPU 워크스페이스)에서 AI Hub 수어 데이터를 학습하는 파이썬 패키지 `ml/`을 신설.

- **랜드마크 기반 소형 트랜스포머 채택 (영상 CNN 아님)** — 배정 자원이 `h200-mig-1g.35gb`, 즉 MIG 1g 슬라이스라 **메모리 35GB / 연산 약 1/7**의 비대칭 구조다. 영상 3D CNN은 이 자원에서 에폭당 수 시간으로 비현실적. 랜드마크 시퀀스(프레임당 155 float)는 입력이 영상의 ~1/2000이라 에폭 수 분이고, 그대로 브라우저 실시간 추론(1MB 미만)까지 이어진다. Conv1d 프런트엔드 + Pre-LN 트랜스포머(2.8M 파라미터).
- **특징 일치를 기계 검증으로 강제** — 학습(파이썬)·추론(TS)이 다른 특징을 쓰면 "검증 95%인데 웹캠에선 0%"가 된다. `ml/tools/feature_parity.py`가 Node 22 타입 스트리핑으로 `landmarks.ts`를 직접 실행해 파이썬과 수치 대조(현재 **오차 0.000e+00**, 240프레임). 검사기 자체도 TS를 일부러 되돌려 오차 7.7이 검출되는 것으로 유효성 확인.
- **미검출 랜드마크 인코딩 수정(landmarks.ts)** — 위 검사에서 실제 불일치 발견. 손 미검출 시 TS가 `(0 - 어깨중점) × 스케일`을 넣고 있었다. "손 없음"이 몸 위치마다 다른 벡터가 되는 잡음이라, **0으로 남기고 존재 플래그로만 알리도록** 양쪽 통일. 기존 배포 모델은 합성 학습본이라 회귀 위험 없음.
- **3단 분리(단어 → CTC 연속 → 언어 복원)** — end-to-end SLT는 데이터 요구가 크고 검증이 어렵다. 단어 인식만으로도 실시간 자막 데모가 성립하고, 언어 단계(KoBART)는 키포인트 ETL 전에 라벨 텍스트만으로 선행 학습 가능. `--direction text2gloss`는 현재 규칙 기반 `signAgent.ts`를 데이터 학습 모델로 대체하는 경로.
- **AI Hub `keypoints3d` 발견·활용** — 저장소 17클립 중 **9개에 3D 키포인트(mm, 카메라 좌표)**가 존재. 2D만 쓰면 155차원 중 51채널(z)이 0이라 MediaPipe 입력과 분포가 어긋나는데, 3D를 쓰면 이 갭이 사라진다. 단 깊이 추정 실패 프레임에 `-25,000,000` 같은 값이 섞여 있어 위생 검사(`sanitize_3d`: 깊이 범위·어깨 폭·몸 중심 반경) 필수. 유효 프레임 70% 미만이면 자동 2D 폴백.
- **ONNX 동적 축 함정** — `dynamic_axes`는 torch.export(dynamo) 경로에서 **무시된다**. 그대로 두면 어텐션 reshape에 배치·길이가 상수로 구워져 내보낼 때 쓴 크기 외에는 런타임 오류(`{32,2,384}` vs `{32,1,3,128}`). `Dim.DYNAMIC` + 배치 2 이상으로 내보내고, 검증도 **여러 입력 크기**로 하도록 수정(한 크기만 보면 못 잡는다). 결과: int8 0.82MB, 오차 2.4e-07, 배치 1·3·4 / 프레임 32·97·256 정상.
- **TF.js 대신 ONNX Runtime Web 선택** — PyTorch 트랜스포머 → TF.js 그래프 변환은 단계가 많고 깨지기 쉽다. ORT-web은 PyTorch 1차 지원 대상이고 WASM/WebGPU 백엔드를 모두 갖췄다. 단 **브라우저 연동은 실제 학습 모델이 나온 뒤로 미룸**(그때 입력 파이프라인까지 함께 검증하는 편이 낫다).
- **CPU/GPU 워크스페이스 분리 권고** — GPU 할당량이 1개인데 MediaPipe 추출은 순수 CPU 작업이라, GPU 워크스페이스에서 돌리면 며칠간 GPU를 놀린 채 점유하게 된다. ETL은 CPU 워크스페이스, 학습만 GPU로.
- **볼륨 1 GiB가 최우선 블로커** — 생성 화면 기본값이 `Home 1 GiB`뿐이라 데이터를 한 조각도 못 받는다. 원본/팩/체크포인트용 볼륨 분리 필요(팩은 프레임당 ≈0.5KB로 작아, 450시간 분량도 ~25GB).
- **정확도 수치는 아직 없음(정직 표기)** — 저장소 17클립·55 학습표본으로 전 구간 실행 검증만 완료. 의미 있는 성능은 AI Hub 전량 수신 후에야 말할 수 있다. MediaPipe 재추출 경로는 이 환경에 mediapipe/opencv가 없어 미실행.

## 저장 위치 결정 — 볼륨을 기다리지 않고 /tmp + 홈으로 시작

데이터 볼륨(`sbdata`) 생성은 콘솔에서 사람이 해야 하는 일이라 그걸 기다리면 아무것도 못 한다.
워크스페이스를 다시 뒤져 보니 쓸 수 있는 저장소가 두 개 있었다.

| 경로 | 크기 | 성격 |
|---|---|---|
| `/tmp` | 1.8T (여유 1.2T) | 컨테이너 디스크. 재시작하면 사라짐 |
| `/home/jovyan` | 50G | 볼륨. 재시작해도 남음(워크스페이스 삭제 시 사라짐) |

`/`·`/opt`는 쓰기 불가. `/dev/shm`은 1TB지만 RAM이라 데이터 저장용으로 부적합.

**원본은 /tmp, 결과물은 홈**으로 갈랐다. 기준은 "다시 만들 수 있는가"다. AI Hub 원본은
다시 받으면 되지만, 팩·사전·체크포인트는 다시 만들려면 몇 시간이 든다. 팩은 프레임당
약 0.5KB라 450시간 분량도 ~25GB — 홈 50G 안에 들어간다. `ml/jobs/koren_paths.sh`에
경로를 모아 뒀고, 볼륨을 붙이면 그 세 줄만 바꾸면 된다.

## 실제 파일 목록 확인 — 홈 50G만으로는 애초에 불가능했다

`aihubshell`(공식 CLI, `~/.local/bin`에 설치)로 datasetkey **636**「재난 안전 정보 전달을
위한 수어영상 데이터」의 파일 목록을 확인했다. **API 키 없이 목록 조회(-mode l)가 된다.**

| 파일 | 용량 | filekey | 쓰나 |
|---|---|---|---|
| Training 2.형태소_비수지(json)_TL | **90 GB** | 61895 | ○ 본 학습 데이터 |
| Validation 2.형태소_비수지(json)_VL | **11 GB** | 62028 | ○ 파일럿 |
| Training 수어스크립트_TL | 372 MB | 61896 | ○ ③단계(글로스↔한국어)용 |
| Training 1.키포인트(xml)_TL | 52 GB | 61894 | ✕ 형태소 JSON에 키포인트가 이미 있음 |
| 원천데이터 .mp4 | 합계 2TB+ | — | ✕ |

라벨링데이터만 해도 압축 상태로 101GB다. 풀면 3~5배로 불어난다.
**홈 50G에 원본을 받는 선택지는 처음부터 없었다** — /tmp로 가른 판단이 결과적으로 필수였다.

**파일럿 우선(11GB 검증셋)으로 간다.** 90GB를 먼저 받으면 다운로드에만 몇 시간을 쓰고
나서야 어댑터 문제를 발견하게 된다. 11GB로 ETL·수어자 분리·어휘 통계까지 한 바퀴 돌려
확인한 뒤 학습셋을 받는다. `ml/jobs/run_disaster.sh {pilot|full|script}`.

**남은 블로커는 AI Hub API 키 하나뿐이다.** 목록 조회는 열려 있지만 다운로드(-mode d)는
사용자 계정 키가 필요하고, 데이터셋 이용 신청 승인도 계정에 묶여 있다.

## 정정 — 홈 볼륨 900GiB 확대로 전환, 그리고 zip을 풀지 않기로

앞 항목에서 "원본은 /tmp, 결과물은 홈"으로 갈랐는데, 홈 볼륨을 **900GiB까지 키울 수
있다**는 사실을 확인해 그 전제를 버린다. `/tmp`는 컨테이너 디스크라 재시작하면 사라지는데,
90GB를 몇 시간 걸려 받아 놓고 재시작 한 번에 날리는 위험을 안을 이유가 없다.
원본·팩·체크포인트를 전부 홈 볼륨에 둔다(`ml/jobs/koren_paths.sh`).

**다만 900GiB라도 압축을 다 풀면 위험하다.** 형태소 JSON은 텍스트라 압축률이 높다.
zip 90GB가 풀리면 수백 GB가 되는데, 실측 전에는 상한을 알 수 없다. 그래서 **압축을 아예
풀지 않기로** 하고 `ml/etl/aihub_disaster.py`가 zip을 연 채로 JSON 멤버만 꺼내 읽도록
고쳤다(`collect_inputs` + 프로세스별 `ZipFile` 캐시). `--input`은 디렉터리·zip·zip이 든
디렉터리를 모두 받는다.

- ZipFile 핸들을 **워커 프로세스 안에서 지연 생성**하는 것이 핵심이다. 부모에서 열어
  fork로 물려주면 자식들이 파일 오프셋을 공유해 읽기가 깨진다.
- **푼 것과 결과가 같은지 검증했다.** 합성 재난안전 샘플 6클립을 (a) 푼 디렉터리,
  (b) zip 두 경로로 각각 ETL → `index.jsonl` 레코드 **완전 일치**, 팩 배열 **최대 오차 0.0**,
  수어자 3명 동일 추출(중첩 `metadata`에서 꺼내는 경로도 함께 확인됨).

필요 용량은 zip을 안 푸는 전제로 원본 102G + 팩 25G + 체크포인트 5G ≈ **132G**.
900GiB면 여유가 크다. 반대로 홈이 50G인 채로 시작하면 90GB 다운로드가 도중에 실패하므로
`sb_require_space`로 **받기 전에 막는다**(pilot 40G / full 150G 요구).

남은 블로커는 두 개다 — **홈 볼륨 900GiB 확대**(콘솔), **AI Hub API 키**(마이페이지 발급 +
데이터셋 이용 신청 승인). 둘 다 사람이 해야 하는 일이고, 그 외 코드는 준비됐다.

## AI Hub API 키 위치 확인 + 필요 용량 재계산(앞 항목 정정)

**API 키는 마이페이지에 없다.** 공식 문서를 확인한 결과 발급 버튼은
[AI 데이터 찾기 > **AI 허브 오픈 API**](https://aihub.or.kr/devsport/apishell/list.do?currMenu=403&topMenu=100)
페이지에 있다. 로그인해야 활성화되고(`javascript:fnLoginLink()`), 누르면 키가 발급되어
**사용자 이메일로 발송**된다. 키 자체는 화면에 안 뜬다 — 메일함을 봐야 한다.

키 발급과 다운로드 권한은 별개다. `aihubshell 가이드`(공식 PDF)에 이렇게 적혀 있다:
"aihubshell 프로그램은 **데이터셋 다운로드 승인이 완료되어야** 다운로드가 가능합니다."
즉 데이터셋 상세페이지(dataSetSn=636)에서 활용 신청 → 승인까지 받아야 한다.
목록 조회(-mode l)만 승인 없이 열려 있다.

**필요 용량을 정정한다.** 앞 항목에서 "zip을 안 푸니 132G면 된다"고 적었는데, 그건
평상시 점유량이고 **내려받는 도중에는 3배까지 부푼다.** aihubshell 소스를 읽어 확인한 순서:

    ① download.tar 수신            90G
    ② tar -xvf → .part 조각 풀기  +90G = 180G
    ③ 조각 병합해 zip 복원        +90G = 270G  ← 최대
    ④ 조각·tar 삭제                      → 90G (zip만 남음)

공식 가이드의 "다운로드 받을 데이터의 2~3배 이상의 용량을 확보하시길 바랍니다"와 일치한다.
따라서 순간 최대는 **약 320G**. 900GiB면 여유가 크지만 **200G 정도로는 학습셋을 못 받는다.**
`sb_require_space` 요구치를 pilot 45G / full 320G로 고쳤다.

한편 ③에서 나오는 최종 산출물은 **원래의 zip 그대로**다(aihubshell은 tar와 분할 조각만
풀고 zip은 건드리지 않는다). 그래서 "zip을 열어 둔 채 멤버만 읽는" ETL 전략은 그대로 유효하다.

## API 키 동작 확인, 그리고 **AI Hub의 `.zip`은 실제로 7z였다**

발급받은 키로 가장 작은 파일(수어스크립트_TL, 372MB)을 실제로 받아 키·승인·경로를 한 번에
검증했다. 다운로드 정상(HTTP 200), aihubshell이 `download.tar` → `.part0` → 병합 순으로
처리하고 **최종 산출물로 `*.zip`을 남기는 것**까지 앞 항목의 예측과 일치했다.

**그런데 그 `.zip`이 zip이 아니었다.**

    $ file 수어스크립트_TL.zip
    7-zip archive data, version 0.4
    $ unzip -l 수어스크립트_TL.zip
    End-of-central-directory signature not found.

확장자만 `.zip`이고 내용은 7-Zip(LZMA2)이다. 파이썬 `zipfile`도 `unzip`도 열지 못한다.
앞 항목에서 만든 "zip을 열어 둔 채 멤버만 읽는" 경로는 **실데이터에서 한 건도 처리하지
못했을 것이다.** 합성 zip으로만 검증한 탓에 놓쳤다 — 실제 파일을 받아 보고서야 드러났다.

더 나쁜 것은 압축 방식이다:

    Method = LZMA2:24   Solid = +   Blocks = 1

**통짜(solid) 압축**이라 멤버 하나를 꺼내려 해도 앞에서부터 전부 풀어야 한다. 파일마다
따로 여는 방식은 O(n²)가 되어 90GB에서는 사실상 끝나지 않는다.

**대응 — 한 번만 훑는 스트리밍으로 바꿨다.**
- 확장자를 믿지 않고 **매직 바이트로 판별**(`archive_kind`: `7z¼¯'\x1c` / `PK\x03\x04`).
- 7z는 `stream_7z`로 처음부터 끝까지 **한 번만** 훑으며, py7zr `WriterFactory`로 멤버가
  다 풀리는 순간(`Py7zIO.close()`) JSON을 넘기고 버퍼를 비운다. 메모리가 일정하게 유지된다.
- 압축 해제는 순차(1스레드)지만 무거운 좌표 변환은 프로세스 풀로 넘긴다. 넘기지 못한
  JSON이 쌓이지 않도록 in-flight를 `workers×2`로 제한한다.
- JSON이 아닌 멤버(xlsx·mp4)는 `NullIO`로 흘려보내 메모리에 담지 않는다.
- **검증**: 같은 6클립을 낱개 JSON / 진짜 zip / solid 7z 세 경로로 각각 ETL →
  `index.jsonl` 완전 일치, 팩 배열 최대 오차 0.0.

`schema_report.py`도 같은 이유로 실데이터에서 조용히 실패하고 있었다("JSON을 찾지
못했습니다"). 7z를 열도록 고치고, 디렉터리를 주면 **안의 아카이브까지 들여다보도록** 했다.
필요한 개수를 채우면 예외로 중단시킨다 — 구조만 보려다 90GB를 끝까지 푸는 사고를 막는다.

## 수어스크립트(372MB)만으로 ③단계를 지금 학습할 수 있다

받아서 열어 보니 형태소 JSON이 아니라 **xlsx 131개**였다(`04_수어스크립트/{1.tact,2.untact}_morpheme/`
아래 재난 유형별). 시간축 표 형식이고, 한 파일에 15만 행 넘게 여러 클립이 쌓여 있다.

    r1  Information / File name        : NIA_SL_G1_COLDWAVE000010_1_TW07.js
    r2               Korean sentence   : 오늘 21시부로 한파가 예상되오니 …
    r19 sign_gestures_both / gloss_id  : 오늘1        start 1.823  end 2.275
    r22 sign_gestures_both / gloss_id  : 밤1, 시:9시  start 2.385  end 3.153
    …
    (비수지 Ci·EBf·Mmo·Mctr 층도 같은 형식으로 들어 있다)

즉 **한국어 문장 ↔ 시간순 글로스 시퀀스 쌍**이 그대로 있다. 키포인트가 필요 없는
③ `train_gloss2text --direction text2gloss`는 **90GB를 기다리지 않고 이 372MB만으로
학습 가능하다.** 현재 앱의 규칙 기반 `signAgent.ts`를 대체하는 경로이기도 하다.
xlsx → (한국어, 글로스열) 추출기는 아직 없다 — 다음에 만든다.

## 홈 볼륨 900GiB 반영 확인, 파일럿 착수

콘솔에서 홈 볼륨을 900GiB로 키운 것이 워크스페이스에 즉시 반영됐다(`df`: 900G, 여유 899G).
볼륨 ID(`csi-vol-2802f6dc…`)가 이전과 같아 **그 자리에서 확장**된 것이고, 먼저 받아 둔
수어스크립트도 그대로 남아 있다. 재시작 불필요. 검증셋(11GB) 다운로드를 시작했다.

## py7zr 콜백은 **멀티스레드로 불린다** — 블록 수에 따라 갈린다

파일럿 ETL 진행 로그에 `2500개 처리`가 여섯 번 찍혔다. 카운터 경쟁이라는 뜻이다.
py7zr `Worker.extract`를 읽어 보니 **`numfolders == 1`이면 단일 스레드, 여러 개면 블록별
병렬**이다. 아카이브마다 다르다:

    수어스크립트_TL (373MB)      Blocks = 1    → 단일 스레드
    형태소_비수지 VL (12GB)      Blocks = 26   → **병렬**

즉 작은 아카이브로만 시험하면 이 차이가 드러나지 않는다. `stream_7z`의 카운터·대기열에
락을 걸었다. **결과 대기(`future.result()`)는 락 밖**에서 한다 — 안에서 기다리면 해제
스레드가 전부 막혀 스트리밍이 멈춘다. 풀리는 순서는 보장되지 않지만 마지막에 id로
정렬하므로 무관하다. 락 추가 후 3경로 동일성 재검증 통과.

## 수어스크립트 xlsx 추출기 — `ml/etl/aihub_script.py`

xlsx에서 (한국어 문장, 시간순 글로스열)을 뽑아 `index.jsonl`로 낸다. 키포인트가 없으므로
`npz`는 빈 문자열이다(`train_gloss2text`는 팩을 읽지 않는다).

**열 짝짓기 함정 — 처음에 틀렸다가 원본 대조로 잡았다.** 표가 시간축이라 값이 *자기
시각*의 열에 놓인다. 그래서 같은 항목인데도 열이 어긋난다:

    r19 gloss_id  오늘1(2열)
    r20 start(s)  1.823(2열)
    r21 end(s)    2.275(**3열**)

열을 기준으로 짝지으면 끝 시각을 못 찾고 **조용히 start와 같은 값이 된다**(글로스 길이가
전부 0이 되는데 에러는 안 난다). 열은 눈금일 뿐이고 짝은 **왼쪽부터 나온 순서**로 맞는다.
고친 뒤 원본 r19~r24와 대조해 `오늘1 1.823→2.275`, `밤1 2.385→2.878`, `시:9시 3.153→3.55`
모두 일치. 가정이 깨지면 조용히 틀리지 않도록 개수 불일치를 `MISMATCH`로 센다
(COLDWAVE 3개 파일 11.9만 글로스에서 0건, `end<=start` 12건 0.01%, 지속 중앙값 0.429초).

**분할은 한국어 문장 기준이 기본**(`--split-by text`). 같은 문장을 여러 수어자가 1:1~1:3으로
연기하므로 클립 단위로 나누면 같은 문장이 학습·평가 양쪽에 들어가 성능이 부풀려진다.
`prepare.py --split-by signer`와 같은 이유다.

COLDWAVE 3개 파일만으로 클립 5,744개 · 서로 다른 문장 3,948개 · 글로스 어휘 2,111종.
전체 131개 파일이면 ③단계 학습에 충분한 규모다.

## 아카이브 처리를 `ml/signbridge/archive.py`로 공용화

`archive_kind`(매직 바이트 판별) · `find_archives` · `stream_archive` · `member_names`를
모듈로 뺐다. 형태소 JSON(aihub_disaster)과 수어스크립트 xlsx(aihub_script)가 같은 함정
(확장자 위장·통짜 압축)을 공유하므로 한 곳에서 관리한다.

## 파일럿 완주 — **첫 실데이터 수치** (검증셋 11GB)

지금까지 "실데이터가 없어 어떤 숫자도 의미 없다"고 적어 왔다. 그 단계가 끝났다.
AI Hub 검증셋(형태소 JSON 12GB, 7z)으로 다운로드 → 팩 → 수어자 분리까지 완주했다.

    클립            20,091개   ← 아카이브 헤더의 JSON 개수와 **정확히 일치**(유실 0)
    글로스 구간     321,148개
    수어자          51명
    3D 깊이 포함    11,719개 (58%)
    재난 유형       44종 (WEATHER 5,952 · 감염병 1,291 · 호우 920 …)
    팩 크기         1.8GB      ← 학습셋(약 8배)을 더해도 20GB 안쪽

**어휘 규모 — 첫 분기점을 이 수치로 정한다.**

    관측 글로스        8,484종
    5회 이상(학습대상) 2,858종
    학습 표본          260,863개
    토큰 커버리지      96.31%

2,858종으로 전체 토큰의 96%를 덮으므로 `--min-count 5`를 그대로 간다. 어휘를 더 늘려도
꼬리 4%를 위해 희소 클래스를 잔뜩 들이는 셈이라 이득이 없다.

**수어자 분리가 실제로 작동하는지 직접 확인했다.** 이것이 이 프로젝트에서 가장 조용히
무너지는 지점이다(metadata 중첩 → signer 공백 → clip 해시로 대체 → 정확도 부풀림).

    train ∩ val  = 0명
    train ∩ test = 0명
    val  ∩ test  = 0명

즉 아래 정확도는 **다른 사람의 수어를 처음 보고 맞힌 것**이다.

## 베이스라인 학습 착수 — 첫 정확도

    표본 260,860 / 검증 12,742 · 클래스 2,860 · 파라미터 3.50M
    [1/40] loss 5.7914 acc 0.201 | val top1 0.418 top5 0.592 | 157.3s

에폭당 157초, 40에폭 약 1시간 45분. **1에폭 만에 검증 top1 41.8%**(2,860지선다).
여기서는 아직 결론을 내지 않는다 — 최종 수치는 학습이 끝난 뒤에 적는다.

주의: 이 데이터는 공식 배포의 **Validation 파트**다. 학습셋(90GB)이 도착하면 그쪽으로
다시 학습해야 하며, 지금 숫자는 파이프라인이 실데이터에서 동작한다는 증거이지
최종 성능이 아니다.

## 학습셋(90GB) 파이프라인 완주 + 수어스크립트 전량 추출 — 야간 자율 작업

**학습셋 (형태소 JSON TL, zip 90GB → 압축 안 풀고 스트리밍)**

    클립          160,801개 (글로스 구간 2,569,926개)
    수어자        53명 · 3D 깊이 94,483개 (59%)
    어휘          21,753종 관측 → min-count 5로 8,145종, 토큰 커버리지 98.94%
    학습 표본     2,151,092 세그먼트
    분할          train 135,477 / val 6,617 / test 18,707 (수어자 분리)
    불량 클립     어깨 붕괴 9개 (0.006%) — feature_health가 자동 표시

디스크 순간 최대는 예측 범위 안이었다(192G+ 사용, download.tar 96.5GB + 조각 + zip).
900GiB 확장이 정확히 필요했다.

**같은 DATA_ROOT를 파일럿과 공유한 부작용** — full ETL이 `index.jsonl`·`vocab.json`을
덮어써서 파일럿 인덱스가 사라졌다. 학습 중이던 iso-pilot은 시작 시점에 메모리로 읽었고
체크포인트에 vocab을 자체 저장하므로(`best.pt`의 `vocab` 키) 영향 없다. 파일럿 팩
2만 개는 index에서 빠진 채 packs/에 남아 있는데, 공식 Validation 파트라서 오히려
**완전 별도 평가셋**으로 나중에 쓸 수 있다. 교훈: 다음부터 데이터셋 파트별로 out 디렉터리를
분리한다.

**수어스크립트 xlsx 131개 전량**

    클립          200,874개
    한국어 문장   148,824개 (서로 다른)
    글로스 어휘   26,125종 (# 장식 포함 표기 — normalize_gloss가 벗기면 JSON 표기와 일치)
    분할(문장 해시) train 160,689 / val 20,157 / test 20,028
    구조 이상     gloss/start 개수 불일치 32행 (전체의 0.01% 미만)

**파일럿 베이스라인(iso-pilot) 결과 — 이 프로젝트의 첫 정확도 수치**

    검증 top1 74.2% / top5 88.9%  (2,860 클래스, 수어자 분리, 40에폭)
    표본: 공식 Validation 파트 26만 세그먼트 · 8~11에폭부터 완만, 이후 +3%p

**이어서 시작한 것 (동시 실행, MIG 35GB 공유)**
- `iso-v1`: full 데이터 8,145 클래스 · 215만 세그먼트 · 60에폭. 에폭당 약 20분 예상.
- `t2g-v1`: KoBART text2gloss, 16만 쌍 · 4에폭 · batch 32. 규칙 기반 signAgent.ts를
  대체할 후보. 컴퓨트를 나눠 둘 다 느려지지만, 서로 다른 병목(로더 vs 행렬곱)이라
  합계 처리량 손실은 감내할 만하다.

## ③단계 첫 결과 — KoBART text2gloss 4에폭 (t2g-v1)

16만 쌍 · 4에폭 · val loss 0.90→0.61. 임의의 재난 문장에 대한 생성 확인:

    입력: 내일 아침 서해안에 대설주의보가 발효될 예정입니다. 차량 운행에 유의하세요.
    생성: 내일1 아침1 서쪽3 지도1 인근1 눈내리다2 거세다1 주의보2 계획2 운전1 조심1 …

**앞부분(한 문장 분량)은 정답 수준이다.** 학습 데이터에 없는 문장인데 시제·장소·기상
현상·행동 지시가 모두 옳은 글로스로 나온다. 규칙 기반 signAgent.ts로는 불가능한 수준.

**남은 문제는 종결(EOS) 미학습** — 문장이 끝나지 않고 변형 번역을 이어붙인다.
데이터 진단: 1:1 번역 148,885 / 1:2 34,763 / 1:3 17,226 클립, 같은 문장에 최대 4개의
서로 다른 번역이 붙는다(35K 문장이 2~3개). 글로스 길이는 중앙값 14~18로 길지 않아
타깃 길이 문제는 아니고, 4에폭 학습 부족이 주원인으로 판단. val loss가 아직 하강 중이라
best에서 이어서 6에폭 추가 학습(t2g-v2). 생성 시 no_repeat_ngram_size=3은 유지
(글로스열에 실제 2-gram 반복이 있어 2는 쓰면 안 된다).

부수 수정: 샘플 생성부에도 token_type_ids 제거(collate만 고치고 생성부를 놓쳐
학습 완주 후 정성 출력 단계에서 크래시 — best는 이미 저장돼 손실 없음).

## iso-v1 중간 경과 (참고용, 최종 아님)

    [1/60] top1 59.4%  [3/60] 70.4%  [4/60] 72.6% (8,147 클래스, 수어자 분리)

4에폭 만에 파일럿 40에폭 최종(74.35%, 단 2,860 클래스)에 근접. 데이터 8배의 효과가
분명하다. 에폭당 약 24분(KoBART와 GPU 공유 중).

## 꼬리 폭주의 진범 — KoBART 라벨에 EOS가 없었다

t2g-v2가 과적합 국면(val 0.6083→0.6243)에 들어가는데도 생성 폭주가 안 잡혀서 원점부터
다시 봤다. 토크나이저를 직접 확인한 결과:

    tok("오늘1 밤1 춥다1").input_ids → [14864, 248, …, 248]   ← BOS도 EOS도 없다

**KoBART 토크나이저는 특수 토큰을 자동으로 붙이지 않는다.** 라벨에 EOS가 한 번도
등장하지 않았으니 모델이 "여기서 끝"을 배울 수 없었다. 반복 억제(no_repeat_ngram)나
length_penalty는 증상 완화일 뿐이었고, val loss가 0.61에서 바닥을 친 것도 이 결함의
상한이었을 가능성이 높다.

수정: collate에서 토큰 레벨로 max_target-1까지 자른 뒤 EOS를 직접 붙인다. 문자열에
"</s>"를 붙이는 손쉬운 방법은 긴 타깃이 잘릴 때 EOS부터 사라져서 쓰지 않았다.
t2g-v2는 중단(가치 없음), v1 best에서 EOS 수정판으로 t2g-v3 6에폭 재학습 시작.

## EOS 수정 확인 — text2gloss가 데모 가능 수준에 도달

t2g-v3(EOS 수정판) 1에폭 만에 생성 폭주가 완전히 사라졌다. max_length 여유를 128까지
줘도 스스로 종결한다:

    입력: 내일 아침 서해안에 대설주의보가 발효될 예정입니다. 차량 운행에 유의하세요.
    생성: 내일1 아침1 지도1 서쪽3 길3 눈내리다1 주의보2 계획2 운전1 조심1          ← 10글로스, 종결

    입력: 규모 4.2 지진이 발생했습니다. 낙하물에 주의하시고 야외로 대피하세요.
    생성: 지진1 4 마침표1 2 사고1 물건1 여러가지2 무너지다0 조심1 밖1 도망1 부탁1   ← 규모 4.2까지 정확

학습 데이터에 없는 문장에서 시제·장소·재난종류·행동지시를 옳은 글로스로 뽑고 알아서
끝낸다. **규칙 기반 signAgent.ts를 대체할 조건을 갖췄다.** 남은 5에폭으로 더 다듬는 중.

## 교차 평가 — 파트 간 일반화 수치 (iso-v1 중간 16에폭 체크포인트 기준)

`ml/eval_isolated.py` 신설 — 체크포인트를 다른 파트 데이터로 평가하며 **어휘 커버리지를
반드시 병기**한다(커버리지 없는 정확도는 부풀림이다). 공식 Validation 파트를
`ksl-valpart`로 재구축(20,091클립, 학습에 단 한 번도 안 씀)해 평가했다.

수어자 구성을 확인하니 **Training·Validation 파트 간 수어자 38/51명이 겹친다** —
AI Hub의 파트 분할은 수어자 분리가 아니다. 그래서 두 단계로 나눠 쟀다:

    ① Validation 파트 전체 (본 수어자의 새 클립):    top1 79.3% / top5 91.0% (커버리지 98.7%)
    ② 그중 학습에 없던 수어자 13명만 (3,203클립):     top1 76.8% / top5 90.8% (커버리지 98.9%)

①-② 격차가 2.5%p에 불과 → 모델이 수어자 버릇에 크게 의존하지 않는다.
②가 "완전 미본 수어자 + 다른 촬영 파트"라는 **가장 엄격한 기준의 현재 수치**다.
iso-v1이 아직 17/60에폭이므로 최종 수치는 학습 완료 후 다시 잰다.

참고: iso-pilot을 valpart로 평가하면 84.8%가 나오는데 이는 **자기 학습 데이터 평가**라
무의미하다(iso-pilot이 Validation 파트로 학습했음). 스크립트 주석에도 박아 뒀다.

## t2g-v3 완주 — 검증셋 대조에서도 근접

최저 val 0.6058(1에폭 시점 best). 검증 샘플 정답 대조:

    입력: 금일 14시 장흥군 한파경보 발효, 노약자 외출자제 …
    생성: 오늘1 저녁1 시:2시 장흥 지역1 갑자기1 춥다1 경보1 늙다1 … 하지마1 수도꼭지1 …
    정답: 오늘1 오후1 시:2시 장흥 지역1 갑자기1 온도내려가다1 춥다1 …

시각·지명·재난·행동지시가 대체로 맞고, "선별진료소→코로나검사1"처럼 의미 보존 대체도
한다. 오차는 저녁1/오후1 수준. 서빙은 이미 /t2g로 연결돼 있다.

## ONNX 내보내기 — 8,147클래스에서 경로 검증, int8은 쓰지 않기로

iso-v1 중간 체크포인트로 내보내기 전 구간 확인: fp32 20.5MB(PyTorch 대비 오차 6.9e-05),
배치·길이 동적 축 정상. 파일럿 때 0.8MB였던 것이 20MB가 된 이유는 분류 헤드가
8,147클래스로 커져서다(256×8147 가중치 ≈ 8.3MB).

int8 동적 양자화는 **실측으로 기각**했다. 미본 수어자 세그먼트 2,000개로 잰 결과:

    fp32 top1 78.1% / int8 77.2% (−0.9%p, argmax 일치 95.0%)
    크기 절감은 20.5→18.7MB (10%)뿐

트랜스포머 몸통이 아니라 거대한 분류 헤드가 지배하는 구조라 양자화 이득이 없다.
브라우저 연동은 fp32로 간다. 20MB는 기본 아바타(11MB)보다 크지만 1회 다운로드라 수용.
(어휘를 데모용 상위 N으로 줄인 경량 헤드 변형은 나중에 필요하면 만든다.)

## 사이트를 '실시간 수어번역 AI'로 보이게 만든 개선 묶음 (야간 2차)

사용자 지적: "문장을 선택해서 클릭하는 걸 아바타가 보여주는 것에 불과해 보인다."
내부가 AI여도 화면이 플레이어 꼴이면 소용없다. 다음을 추가했다.

1. **실시간 관제 화면(LiveConsole)** — AI Hub 실데이터에서 뽑은 재난문자 246건·41유형이
   자동 수신되어 수신→번역→합성→송출 단계로 처리되는 과정을 표시. 각 단계 시간은
   performance.now() **실측**(지어낸 숫자 금지). 병목 실측으로 두 건 수정:
   조각 순차 로드 14.7s→9ms(병렬 선로딩), 서버 부재 재확인 매건 850ms→1회 캐시.
2. **KOREN 다지점 송출 시각화** — 송출 단계에서 NIA POP 10개소 순차 점등.
   지연 수치는 "회선 연동 전 추정치"로 명시. 전송량(관절 2.4KB, 영상 대비 1/2000) 병기.
3. **음성 인식 중 실시간 글로스 미리보기** — 말하는 도중 사전으로 훑어 단어가 쌓이는
   것을 표시(합성은 확정 문장으로만).
4. **웹캠 인식을 실데이터 모델로 전환** — ONNX Runtime Web으로 iso-v1(8,147종,
   val top1 76.7%)을 브라우저 구동. 실행 시점 함정 2건을 실구동으로 잡음
   (wasm 미번들 → public/ort 정적 배치 13MB, wasmPaths 상대경로 → baseURI 절대화).
   ?selftest=onnx 진단 경로 추가. 추론 18ms. UI에 모델 정체·정확도 명시,
   합성 모델 선택 시 "실제 수어 인식 불가" 문구.
5. **양방향 왕복** — 인식된 수어 → Q&A 답변 → CustomEvent로 아바타 섹션에 전달 →
   답변을 수어로 재생. 기획안의 '양방향 소통'이 처음으로 닫혔다.
6. 자막 오버레이·연속 방송 모드·문장 수 표기 정정("문장 수 제한 없음") 등.

정적(서버 0대) 기준으로 전 기능 동작을 Playwright 실구동으로 검증했다.

## 「수어 영상」(dataset 103) 편입 — 일상 어휘 확장

승인돼 있던 두 번째 데이터셋을 확인·수신 시작했다. 구조가 재난안전과 다르다:
형태소(라벨)와 키포인트가 **별도 zip**이고, 형태소엔 한국어 문장이 없다(글로스 타임라인만).
따라서 언어쌍이 아니라 **인식 어휘 확장용**이다.

- 형태소 6종(224MB) 수신 완료, `aihub_sl` 판정 통과(schema_report)
- WORD 어휘 실측: **2,963종 · 약 24만 클립**. 재난안전 어휘와 정규화 비교 시
  겹침 409종 → **순수 신규 일상 어휘 2,554종**(감기·가렵다·가루약·가발 …)
- 키포인트는 WORD 4개 zip(41GB)만 선별 수신 중. 전량(sen 포함 ~300GB)은
  WORD 결과를 보고 결정
- 통합 시 인식 어휘 8,145 → 약 10,700종. 재난 도메인을 넘는 첫 확장

주의: 라벨 표기가 다르다(재난안전 `꺼지다1` / 수어영상 `꺼지다`). 통합 학습 전에
표기 정규화 정책을 정해야 한다 — 안 하면 같은 수어가 두 클래스로 갈라진다.

## 수어→텍스트의 언어 단계 개통 + 의미 드리프트 발견

g2t-v1(글로스→한국어, KoBART) 1에폭 결과가 예상을 넘는다 — 공문체 재난문자를
그대로 복원한다. 그러나 실측에서 **안전에 치명적인 오역 유형**을 발견했다:

    글로스: 지진1 발생1 밖1 도망1 부탁1   (뜻: 밖으로 대피하라)
    복원:   "지진이 발생하였으니 외출을 자제하여 주시기 바랍니다"  ← 정반대

원인: 학습 문장 대부분이 "…자제하시기 바랍니다"체라, 모델이 글로스 의미보다
재난문자 템플릿 분포에 끌린다. 대응:
1. /g2t 응답에 글로스 원문 필수 포함, 화면은 원문 병기 강제(코드 주석으로 박음)
2. 남은 3에폭이 개선할지 관찰. 안 되면 글로스 충실도 페널티(coverage 제약) 검토
3. 시연 대본에서 이 한계를 숨기지 않고 "복원문+원문 병기" 설계로 설명

이로써 수어→텍스트 방향도 언어 단계까지 개통: 단어 인식(77.5%) → [CTC 대기] →
문장 복원(개통). CTC가 끝나면 연속 수어 → 자연어 문장 전체 사슬이 붙는다.

## 부분열 증강 실험 기각 — 드리프트 대신 환각을 만들었다

가설(짧은 글로스열 분포 확장)대로 g2t-v2를 학습했으나, 1에폭 결과에서 **더 나쁜
실패 유형**이 나타났다: 글로스에 없는 구체 지명·수치를 창작한다.

    입력: 지진1 발생1 밖1 도망1 부탁1
    v2 : "경북 경주시 남남서쪽 6km 지역 규모 4.3 지진발생, …"   ← 전부 지어낸 것

원인 해석: 부분열(입력 축소) ↔ 전체 문장(타깃 유지) 매핑을 학습시키니, 모델이
"없는 정보는 그럴듯하게 채우는" 방향으로 최적화됐다. 안전 안내에서 가짜 수치·지명은
의미 드리프트보다 위험하다. **v2 학습 중단, 서빙은 v1 유지.**

대신 방어를 기계화했다: /g2t 왕복검증에 **환각 수치 가드** 추가 — 복원문의 숫자가
입력 글로스에 없으면 low_confidence. v2 출력으로 실검증(가드 발동 확인).
교훈: 증강은 입력만 줄이면 안 되고 타깃도 함께 줄여야 하는데, 그런 요약 쌍은
데이터에 없다. 짧은 열 대응은 (a) 왕복검증+환각가드로 방어하고 (b) 근본적으로는
연속 인식(CTC)이 애초에 전체 문장 글로스열을 내게 하는 쪽이 맞다.

## 통계 사전에 표제어 직결 규칙 추가 — Dice 잡음 오역 방어 (2026-08-17)

실검증 중 `조심 → 비닐하우스0` 오역을 발견했다. Dice 공기 통계가 드문 낱말에서
엉뚱한 후보를 1위에 올린 것이다. 글로스 ID가 "조심1"처럼 **한국어 표제어+번호**라는
점을 이용해, 표제어가 낱말과 정확히 일치하면 그 글로스를 무조건 1순위로 앞세우고,
Dice에 안 잡힌 표제어 일치 글로스 2,013개도 사전에 편입했다(5,956→7,969낱말).
종단 커버리지 93.0→93.3%, 완전표현 149→153/246. 수치보다 오역 제거가 본질이다.

같은 맥락에서 번역 단계에서 조용히 사라지던 미매칭 낱말(주로 지명·기관명)을
`unmatched`로 반환하게 하고, 이용자 화면이 **낱말 카드**(호박색 큰 글씨)로 띄운다.
지문자 데이터(FINSP)는 수어영상(103) 목록에 없음을 확인 — 동작 표현은 불가하므로 ⚠️ **이 판단은 2026-08-18에 뒤집혔다**(맨 아래 참조: 실데이터 표기가 `FS`라 못 찾았을 뿐 17,000클립이 있다). 당시엔
정보 손실 방지를 UI로 해결했다. 사전 탭에는 초성 검색(ㅈㅈ→지진)을 추가했다.

## 수어영상 WORD 12,000클립 뱅크 편입 — 미터 단위 반올림 함정 (2026-08-17)

`ml/etl/slword_to_bank.py` 신설. 팩 index에서 글로스·구간을 읽고 좌표는 원본
프레임 JSON에서 다시 뽑는다(팩은 MP33 변환본이라 OpenPose 25점 복원 불가 —
build_gloss_bank과 같은 이유). 시험 편입에서 두 번 데었다:
1. split_keypoints가 좌표(N,3)+신뢰도를 **따로** 반환하는데 (N,4)로 이어붙여
   좌표가 점 경계를 넘어 섞였다(pose 전부 0으로 보임).
2. 수어영상 3D는 **미터 단위**(±3m)라 재난안전(mm·픽셀)처럼 소수 1자리로
   반올림하면 0.1m 해상도가 되어 손 동작이 통째로 사라진다. 좌표 중앙값이
   10 미만이면 4자리, 아니면 1자리로 **크기 기반 자릿수**를 쓴다.
   ("절대 임계값을 쓰지 말 것" 함정 목록의 반올림 판이다.)
검증: '고민' 클립을 브라우저 아바타로 재생해 동작 확인(어깨 정규화가 단위 흡수).
부수 확인: 서비스워커 활성 후엔 playwright page.route가 우회된다 —
데이터 주입 테스트는 service_workers='block'으로.

## 일상어휘 대확장 완료 — 웹 사전 9,500종, 종단 94.0% (2026-08-17)

WORD 12,000클립 편입 결과 뱅크 25,627종(신규 2,585·교체 643). 웹 추출을 5,000→9,500종
으로 올렸다(출현 커버리지 98.8%, 186MB — GH Pages 상한 안). 번역 사전 10,776낱말.
종단 실측 94.0% · 완전표현 157/246. 약국·접수·병원·택시 등 생활 어휘가 자동 번역에
들어왔고, '약국'을 브라우저 아바타로 재생해 mm 환산 경로까지 검증했다.

구어체 어미(할까요·세요·습니까 등)를 어간 규칙에 추가(파이썬·TS 동일 유지)하고
용언 표제어를 "-다"형·어간형 두 키로 직결했다. 장소 모드 문구의 완전 자동번역은
3/30에 그침(활용형 형태소 분석 없이는 한계) — 수동 글로스 매핑을 유지하는 근거.

## 번역 품질 대수술 — 숫자 수어, 제외어, 복합 키 프루닝, 시드 (2026-08-17)

실사용 매핑 전수 감사(246건 빈도순)로 오역을 유형화해 네 가지를 고쳤다.
1. **숫자**: 한글 토큰화에서 통째로 사라지던 숫자를 한국식 수 읽기 글로스열로
   (342→삼백사십이, 4.0→사점영, 전화번호→자릿수). 점·년 글로스를 웹 뱅크에 주입.
   숫자 뒤 한 글자 단위(시·분·일·명·층·도·년)는 조사가 붙어도 살린다.
2. **번역 제외어**(STOP_WORDS): 바랍니다·주시기·있습니다류 문법·공손 표현은 수어에
   대응이 없는데 Dice가 잡음 매핑(주시기→자동차2, 있습니다→기차1)을 만들었다.
   빌더에서 키 생성 자체를 막고, TS에서도 같은 목록으로 건너뛴다.
3. **복합 키 프루닝**: "대피바랍니다→낚시1" 같은 통짜 키 1,231개를 제거 — 분해
   가능한 키는 런타임 최장일치 분해가 정확하다(제외어 조각은 소비만). 빌더의
   제거 판정과 런타임 분해 규칙을 반드시 같게 유지할 것(비대칭이면 커버리지 추락).
4. **수동 시드 7개**: 통계가 못 잡는 고빈도 확정 대응(곳으로→장소1, 여진→지진1 등).

커버리지는 94.6→93.8%로 내려갔지만 **이전 수치엔 오역이 "표현됨"으로 세어져
있었다** — 지금 수치가 정직하다. 검증 문장 "15시 민방위훈련 실시. 주민들은 안전한
곳으로 대피바랍니다"가 오역 0개로 번역됨을 브라우저에서 확인.

## 창구 대화 모드 — 마이크·음성 출력·오프라인 준비 (2026-08-17)

목표를 "재난문자를 수어로"에서 **"병원·택시·관공서에서 폰 하나로 대화"** 로 넓히면서,
빠져 있던 절반을 채웠다. 이전 장소 모드는 직원이 **준비된 카드만** 누를 수 있었고
농인의 답은 **화면으로만** 나갔다. 현장은 그렇지 않다.

1. **직원 마이크 → 자막 + 수어** (`useSpeechInput`을 대화 화면에 연결). 의사는 카드에
   없는 말을 한다. 말하는 중 잠정 자막도 띄운다 — 농인에게 가장 불안한 것은
   "지금 무슨 말을 하는지 모르는 침묵"이다.
2. **농인 답 → 음성 출력**(`useSpeechOutput`, Web Speech Synthesis). 택시 기사는 운전
   중 화면을 못 본다. 브라우저 내장이라 서버·요금·회선이 필요 없다.
3. **소리가 나갔는지 눈으로 확인**시킨다. 듣지 못하는 사용자는 확인할 수단이 없다.
   실측에서 `u.voice` 대입이 예외를 던져 speak 전체가 죽었는데 화면에는 "소리로
   전달했어요"가 떠 있었다 — 전달된 줄 알고 기다리게 하는 **거짓 확인**이 가장 위험해
   `failed` 상태를 따로 두고 실패를 붉게 알린다.
4. **차례 전환**: 하단 카드는 한 쪽만 편다(폰 390×844에서 둘 다 펴면 대화 기록이 0px로
   눌린다). 단 **마이크는 차례와 무관하게 항상 보인다** — 자유 발화가 이 화면의
   가장 중요한 입구라 숨으면 직원이 못 찾는다(e2e가 이 결함을 잡았다).

### 재생기 훅 분리 (`useSignPlayer`)
아바타가 "받기" 탭에만 있어 장소 모드에서 카드를 누르면 **화면이 튕겼다.** 마주 앉아
주고받는 대화에서 화면 전환은 대화 단절과 같다. 번역·합성·프레임 루프를 훅으로 빼고
무대(`SignStage`)를 공유해, 대화 화면이 제자리에서 수어를 보여준다.

### 상용구 글로스 정합 검사 (`ml/tools/check_app_glosses.py`)
동작 사전을 9,505종으로 다시 만든 뒤 상용구 30개 중 **17개**의 글로스가 사라져 병원·
약국·긴급 문구가 재생되지 않았다. 없는 글로스는 조용히 건너뛰고 한국어 원문은 그대로
떠 있어 **화면만 봐서는 정상처럼 보였다.** 실존 글로스로 20건 교정하고, 동작 사전을
다시 만들 때마다 돌릴 검사를 추가했다(`feature_parity`와 같은 성격).

### 오프라인 준비 (`scripts/build-offline-manifest.mjs`, `useOfflineReady`)
서비스워커는 **지나간 응답만** 캐시한다. 집에서 설치하고 병원 지하에서 처음 누른
문구는 캐시에 없고 회선도 없다 — 정작 필요한 순간에 실패한다. 두 단으로 미리 받는다:
필수(상용구 글로스+고빈도 300종+사전, 6.7MB)는 첫 방문 뒤 조용히, 전체(1,200종+아바타,
32.9MB)는 용량을 밝히고 사용자가 누를 때. **회선을 끊은 상태에서 창구 문구가 64프레임
재생됨을 실측 확인**했다.

### 기기 3종 자동 검증 (`scripts/e2e_app.py`)
이 앱의 실패는 "화면은 정상인데 알맹이가 없는" 모양이라 눈으로는 못 잡는다. 재생
프레임 수·소리로 나간 문장·요소 크기를 수치로 확인한다(`data-sign-*` 계측점).
폰 390×844 · 태블릿 820×1180 · 키오스크 1080×1920 전부 통과.
**dev 서버(5173)로 검증하지 말 것** — Vite dev는 실행 중 새로 생긴
`public/data/glosses/` 파일을 index.html로 폴백해(200 text/html) 모든 합성이 실패한다.
배포본에서는 정상이며, 이 차이 때문에 한동안 회귀로 오인했다.

## 일상 대화 번역 — 도메인이 바뀌면 수치를 다시 재야 한다 (2026-08-17)

번역 사전은 재난문자 16만 문장쌍에서 뽑았다. 그런데 앱이 서는 자리는 병원·택시·
관공서 창구이고, **직원이 하는 말은 재난문자가 아니라 일상어**다. 재난 커버리지
93.7%로는 그 자리의 품질을 알 수 없어 창구 문장 58개로 따로 쟀다. **64.9%였다.**
("번호표를 뽑고 기다려 주세요" → 수어 "매일" 한 낱말)

원인이 셋으로 갈렸고 각각 고쳤다.

1. **활용형 미처리** — 빠진 낱말의 대부분이 기다려·찍어·뽑고·오세요·있나요 같은
   활용형이었다. 런타임 어간 추출기를 똑똑하게 만들 수도 있었지만 그러면
   `dictSignAgent.ts`와 규칙을 양쪽에서 같게 유지해야 한다 — 이 프로젝트에서 가장
   찾기 어려운 실패다. 그래서 **빌드 시점에 활용형을 펼쳐 사전 키로** 넣었다
   (한글 조합 계산으로 아/어 축약을 되돌린다: 기다리+어→기다려, 들다→드세요).
   런타임은 한 줄도 고치지 않았다. 활용형 키 12,673개, 사전 6,502→22,068 낱말.
   - 이때 `len(lemma) < 3` 조건이 **2음절 용언 전체**(있다·하다·가다·오다·보다·먹다)를
     걸러내고 있었다. 가장 흔한 동사들이 통째로 빠져 있었다.
2. **웹 동작 사전이 재난 빈도로 선별돼 일상어가 잘림** — 결과·수술·도장·요금·
   안전벨트·알레르기가 전체 사전(25,627종)에는 있는데 웹 사전에는 없었다. 창구에서
   가장 필요한 말들이 "재난문자에 안 나온다"는 이유로 잘린 것이다.
   `ml/data/daily_vocab.txt`(생활 어휘 331개)를 만들어 **빈도와 무관하게** 싣는다
   (`export_web_bank --must`). 웹 사전 9,505 → 10,175종.
3. **한글 수사 미처리** — 재난문자는 숫자를 아라비아 숫자로 쓰지만 **말로 들어오는
   문장은 한글**이다("만 오천 원", "삼십 분", "아홉 시"). 음성 인식이 받아 적는 형태가
   그렇다. 한자어·고유어 수사 파서를 넣었다. 한 글자 수사(일·이·삼·만)는 낱말과
   겹치므로 **뒤에 단위가 올 때만** 수로 본다.

결과: 일상 64.9% → **75.6%**, 재난 93.7% → **94.7%**.

### 규칙 폴백이 수치를 부풀리고 있었다
중간 측정에서 84.5%가 나왔는데, 이는 **사전이 한 낱말도 못 찾은 문장이 규칙 폴백으로
넘어가 `unmatched`를 보고하지 않았기** 때문이다. 규칙 폴백은 한국어 낱말을 그대로
"글로스"라고 내놓지만 동작 사전에 그런 조각이 없어 **아바타가 가만히 서 있는다** —
화면상 정상처럼 보이는 조용한 실패다. 폴백을 끊고 낱말 카드로 정직하게 돌려주도록
바꾸니 **75.6%**가 됐다. 이전 수치가 거짓이었다(94.6→93.8 때와 같은 종류의 정정).

### '월'·'점' 증발
숫자·단위 글로스는 코드에 상수로 박혀 있는데(`UNIT_GLOSS`, `PLACE_GLOSS`), 동작 사전을
다시 만들자 '월'이 31회, '점'이 8회 **번역은 됐지만 동작이 없어 화면에서 사라졌다.**
`check_app_glosses.py`가 이제 코드 상수까지 대조한다. 동작이 아예 없는 단위('살')는
표에서 뺐다 — 없는 것을 쓰는 것보다 없다고 하는 편이 정확하다.

## 수어로 답하기 · 가로 배치 · 유휴 초기화 (2026-08-17)

- **수어 입력**(`SignInputPanel`): 대화 모드에서 농인이 답하는 길이 카드·필담뿐이었다.
  둘 다 한국어를 거친다 — 수어가 모어인 사람에게는 그 자체가 부담이고 카드에 없는 말은
  결국 못 한다. 카메라로 수어를 읽어 **소리로** 내보낸다. 지금 인식기가 낱말 단위라
  화면도 낱말을 쌓는 방식이다(연속 인식이 나오면 인식기만 갈아 끼운다).
  영상은 브라우저 안에서만 처리되며 화면에도 그렇게 적었다.
- **좌우 2단 배치**(lg 이상): 창구에 놓인 태블릿은 대개 가로다. 세로로만 쌓으면
  아바타가 작아지고 대화 기록이 눌린다.
- **유휴 5분 자동 초기화**: 키오스크·창구 태블릿은 여러 사람이 돌려 쓴다. 앞사람이
  "머리가 아파요"라고 답한 대화가 다음 사람 화면에 남아 있으면 안 된다.
- **음성 인식 오류 문구**: "network"가 아니라 "인터넷이 없어 음성 인식을 할 수 없어요.
  아래 질문 카드나 직접 쓰기를 써 주세요"로. 음성 인식은 원리상 서버를 타므로
  **오프라인에서는 동작하지 않는다** — 재난 때 흔한 상황이라 대안을 함께 말해야 한다.

## 수어 어순 반영 · 주석 글로스 배제 · 창구 5곳 추가 (2026-08-17)

### 어순은 규칙으로 짜지 않고 말뭉치에서 잰다 (`ml/etl/build_order.py`)
사전 기반 번역은 **한국어 어순 그대로** 글로스를 늘어놓았다. 한국수어는 어순이 다르다.
재난 문장 20만 개에서 낱말별 평균 위치를 재니 편향이 뚜렷했다.

    앞  군청 0.11 · 경기광주 0.14 · 고양 0.15 · 오전 0.15   (어디·언제가 먼저)
    끝  대비 0.87 · 부탁 0.88 · 조심 0.86 · 도망 0.86       (당부·행동은 마지막)

즉 **[언제·어디] → [무슨 일] → [무엇을 하라]**. 사람이 규칙을 짜 넣는 대신 농인 수어자
들이 실제로 표현한 순서를 그대로 쓴다. 브라우저는 이 표로 **안정 정렬**만 한다 —
편향이 없는 낱말은 원래 자리를 지키고, **숫자와 단위는 한 덩어리로 묶어** 옮긴다
("삼 십 분"이 갈라지면 뜻이 사라진다).
실측: "조심하세요 오늘 경산시에 지진" → **경산 오늘 지진 조심**.

표는 **표제어 기준**으로 만든다. 처음에 글로스 ID로 만들었더니 번역기는 '조심'을
내놓는데 표에는 '조심1'이 있어 한 번도 맞지 않았다(있으나 마나 한 표였다).

### 날짜·시각 주석 글로스가 낱말 2만 개를 오염시키고 있었다
원본 말뭉치에는 `날짜:9월16일`·`시:15`·`시간:2시간` 같은 클립이 있다. 낱말이 아니라
**그 문장에만 해당하는 숫자 주석**인데, 공기 통계가 이걸 자주 1위로 올려 놓았다:

    주택 → 날짜:9월16일 · 많음 → 날짜:12월13일 · 민방위훈련 → 시간:15분 · 일찍 → 시간:2시간

이런 낱말이 **11,974(시:) + 7,112(날짜:) + 810(시간:) ≈ 2만 개**였다. 번역 사전과 웹
동작 사전 양쪽에서 뺐다(1,244종). 숫자·시각은 앱이 직접 읽어 표현한다.
**표현률은 95.5%로 그대로였다** — 커버리지는 이런 오염을 전혀 잡아내지 못한다.

### 복합어 분해를 점수제로
앞에서부터 가장 길게 자르던 방식이 "한파주의보가"에서 '한파주의'(통계 잡음 키)를 먹고
남은 '보가'를 못 붙여 **문장 전체를 번역에서 떨어뜨렸다.** 낱말 전체를 덮는 분해를
찾되 **조각 길이의 제곱합**으로 고른다 — [한파(4)+주의보가(16)=20]이
[한파주(9)+의보가(9)=18]을 이긴다. 빌더의 프루닝 판정도 같은 전체-덮기로 맞췄다
(비대칭이면 지워도 될 키가 남거나 남겨야 할 키가 지워진다).

### 형태론 확장
- 한 글자 명사(약·열·코·입·손·물)가 **`lemma_best`에서 통째로 빠져** 있었다
  (조건 `len >= MIN_STEM`). 조사형을 만들 기회조차 없었다.
- 과거형(왔어요·기다렸습니다)·ㅂ 불규칙(매운·매워)·르 불규칙(눌러·빨라)·
  ㅡ 탈락(아프다→아파)·존대 -시-(아프신지) 추가.
- 표면형이 겹칠 때는 **빈도가 높은 표제어가 가져간다**(아프신지 ← 아프다 vs 신다).
- 한 글자 명사의 조사형은 **Dice를 덮는다** — "물을→끓다1"은 통계 잡음이었다.

### 창구 5곳 추가
은행·식당·경찰서·우체국·마트. 상용구 30 → 59개. 글로스는 전부
`check_app_glosses.py`로 실존 확인했다(처음 지정한 7개는 사전에 없어 교정: 영수증·
봉투·맵다는 동작 자체가 없어 **문구를 바꿨다** — 없는 것을 있는 척하지 않는다).

### 도메인별 실측 (2026-08-17 밤)
| 도메인 | 시작 | 지금 |
|---|---|---|
| 재난문자 246건 | 93.7% | **95.5%** |
| 행동요령 38문장 | 80.8% | **88.6%** |
| 창구 대화 58문장 | 64.9% | **84.5%** |

오역 회귀 사례 25건은 `scripts/check_translation_cases.mjs`가 지킨다.

## 수어 답변을 말이 되는 한국어로 · 인식 후보 고르기 (2026-08-17)

### 낱말 나열이 아니라 문장으로 (`src/agents/glossToKorean.ts`)
수어 인식은 낱말을 하나씩 내놓는다. 그대로 소리로 내보내면 스피커에서
**"머리 어제 아프다"** 가 나온다. 뜻은 통하지만 창구 직원은 한 번 더 되묻게 되고,
농인은 자기 말이 서툴게 들렸다고 느낀다. 용언을 공손형으로 활용해 잇는 최소한만 한다:

    머리 어제 아프다 → **머리 어제 아파요**
    화장실 어디 있다 → **화장실 어디 있어요**
    카드 계산 하다   → **카드 계산해요**

제대로 하려면 글로스→한국어 복원 모델(KoBART)이 필요하고 그건 서버가 있어야 한다.
서버 없이 도는 이 앱에서는 **문장을 지어내지 않고 있는 낱말만 잇는다.** 서버가 붙으면
이 함수 자리에 복원 모델을 끼우면 된다. 회귀 사례 7건으로 지킨다.

### 인식 후보에서 고르기
모델은 낱말 하나를 78.6%로 맞힌다(top-5는 90.8%). 틀린 낱말이 **소리로 나가면** 창구에서
바로 오해가 된다 — "머리"와 "허리"는 진료가 달라진다. 확정된 낱말을 누르면 상위 5개
후보가 펼쳐지고, 하나를 고르면 바뀐다. **손가락 한 번으로 top-1을 top-5로 바꾸는 것**이
모델을 더 키우는 것보다 지금은 확실하다. 전하기 전에 만들어질 문장도 미리 보여준다 —
소리를 못 듣는 사용자는 나간 뒤에는 확인할 방법이 없다.

### 앱에 붙은 모델이 에폭 27짜리였다
60에폭을 돌리는 동안 `public/models/ksl-iso/`에는 **에폭 27**(val top1 0.7746) 모델이
붙어 있었다. 화면상 아무 차이가 없어(인식이 조금 더 틀릴 뿐) 아무도 몰랐다.
에폭 44(0.7866)로 교체하고, 다음부터는 `ml/jobs/deploy_model.sh` 한 줄로 갈아 끼운다.
`ml/jobs/rebuild_data.sh`는 동작 사전 → 번역 사전 → 어순표 → 검사 2종을 순서대로 돈다
(순서를 어기면 사라진 글로스를 가리키는 번역이 남아 아바타가 조용히 서 있는다).

### 검사 스크립트가 조용히 무력화돼 있었다
번역기는 사전과 어순표를 각각 fetch로 받는데, 검사 스크립트의 가짜 fetch가 **모든
요청에 사전을 돌려주고 있었다.** 그래서 어순 검사가 한 번도 실제로 돌지 않았다.
URL을 보고 갈라 주도록 고쳤다 — 검사 도구 자체도 검사 대상이다.

## iso-v1 학습 완료 · 최종 수치 (2026-08-17 18:37)

60에폭 완주. **검증(수어자 분리) top-1 0.7868 · top-5 0.908**(에폭 57).
학습에 전혀 없던 수어자 13명(49,721표본)으로 다시 재니 **top-1 0.7863 · top-5 0.9188**.
검증과 거의 같다 — **수어자에 과적합되지 않았다**는 뜻이고, 처음 보는 사람 앞에서도
같은 성능이 나온다는 근거다(창구에서 쓰려면 이게 정확도 수치 자체보다 중요하다).

앱에 붙어 있던 에폭 27(0.7746) 모델을 에폭 57로 교체했다.
이어서 **iso-v2**(재난+일상 합본, 클래스 13,576 · 학습 표본 217만 · 6.26M 파라미터)가
같은 GPU에서 시작됐다. 끝나면 `ml/jobs/deploy_model.sh ~/sbruns/iso-v2` 한 줄로 붙인다.
그 뒤 CTC(연속 인식)가 이어진다.

소개 페이지의 지표를 **목표치에서 실측치로** 바꿨다(BLEU 목표 → 재난문자 95.5%,
인식 78.7%). 재어 본 숫자가 있는데 목표를 말할 이유가 없다.

## 일상어 클립 전량을 웹 사전에 (2026-08-17 19:2x)

AI Hub '수어영상 단어' 데이터셋에는 농인 수어자가 실연한 **일상 낱말 2,963종**의
클립이 있다. 그런데 웹 동작 사전은 재난문자 출현 빈도로 상위 N종을 고르기 때문에,
**표현할 수 있는데 실리지 않은 낱말이 1,022종**이었다 — 어젯밤·독서실·증인·음료수·
발가락처럼 창구에서 실제로 오가는 말들이다. 표현할 수 있는 것을 빼 두는 것은 순전한
손해라서, 어휘 목록을 그대로 `--must`에 넣어 빈도와 무관하게 싣는다.

    웹 동작 사전 10,175 → 11,438종 · 조각 215MB → 256MB
    번역 사전 124,188 → 141,710 낱말 (gzip 507 → 593KB)

측정용 문장 116개(창구 58 + 행동요령 38 + 재난 246)의 표현률은 그대로다 — 그 문장들에
이 낱말이 없기 때문이다. **커버리지 수치가 안 움직여도 할 가치가 있는 일이 있다**는
예다(반대로, 수치만 좇으면 시험 문장에 맞추게 된다).

목록은 `ml/data/daily_vocab_aihub.txt`(자동 생성)와 사람이 고른
`ml/data/daily_vocab.txt` 둘로 나눠 둔다 — 손으로 관리하는 것과 데이터에서 나온 것을
섞으면 다음 사람이 어느 쪽을 고쳐야 할지 모른다.

## 체감 속도 실측과 미리 데우기 (2026-08-17 19:5x)

창구에서 질문 카드를 눌러 아바타가 움직이기까지의 시간을 쟀다.

    처음 누른 문구(조각을 받아야 함)   약 2,100ms
    이미 받아 둔 문구                  약   450ms

창구에 들어서면 **그 장소에서 쓸 문구는 이미 정해져 있다.** 장소를 고르는 순간 그
장소의 글로스 조각을 조용히 받아 두게 했더니(`player.prewarm`), 직원 안내를 읽는 몇 초
사이에 준비가 끝나 **중앙값 516ms**가 됐다. 4배다.

### 측정을 두 번 틀렸다 — 기록해 둔다
1. 처음엔 6.8초가 나왔다. 알고 보니 **플레이라이트의 클릭이 반환되기까지의 시간**을
   재고 있었다(요청은 클릭이 반환되기 **전에** 이미 시작돼 있었다). 3D 캔버스가 계속
   그려지는 페이지에서 클릭 액션의 사전 검사가 오래 걸린 탓이다.
   → 페이지 **안에서** `performance.now()`로 재도록 바꾸니 실제 값이 나왔다.
2. 그 사이 "오프라인 미리 받기가 요청을 밀어낸다"고 판단해 동시 수를 6→2로 줄였는데,
   미리 받기를 끄고 다시 재 보니 **차이가 없었다.** 원인은 조각 내려받기였다.
   동시 수 축소는 되돌리지 않았다(좁은 회선에서는 그래도 안전한 쪽) — 다만
   **측정으로 뒷받침되지 않는다는 사실을 주석에 적어 두었다.**

교훈은 늘 같다: 고치기 전에 재고, 잰 것이 정말 그것인지 다시 확인한다.

## 홀드아웃을 만들었다 — 튜닝 집합에 맞춘 것은 아닌지 (2026-08-17 20:0x)

창구 대화 표현률을 64.9 → 84.5%까지 올렸는데, 그 58문장을 보면서 사전을 고쳤으니
**시험 문장에 맞춘 것일 수 있다.** 같은 자리(병원·약국·관공서·택시·식당·은행)의 다른
37문장을 새로 써서 재 보니 **75.0%** 였다. 일반화는 되지만 9.5%p 차이가 났다.

빠진 것을 유형별로 보니 대부분 **활용형**이었다(오셨나요·아프신가요·있었나요·될까요·
잡을까요·드시면). 존대·추측·의향 어미와 'ㄹ' 미래형(되+ㄹ까요 → 될까요), 과거+의문형을
활용형 생성에 넣으니 **80.2%**가 됐다. 튜닝 집합은 84.5% 그대로 — 즉 이번 개선은
특정 문장이 아니라 **어미 규칙**을 고친 것이라 새 문장에도 통한다.

문장 집합은 `scripts/audit_sets/`에 넣어 다음 사람도 같은 기준으로 재게 했다.
규칙 하나만 적어 두었다: **홀드아웃을 보고 고치기 시작하면 그 순간 홀드아웃이 아니다.**

## 3단 폴백이 코드에서는 2단이었다 (2026-08-17 20:2x)

`dictSignAgent.ts` 머리말에는 **KoBART(서버) → 사전 → 규칙** 3단이라고 적혀 있었는데,
`KoBartSignAgent`는 서버가 없으면 **규칙으로 바로** 떨어지고 있었다. 규칙 기반은 조사만
떼고 한국어 낱말을 그대로 "글로스"라고 내놓는다 — 화면에 글자는 뜨는데 동작 사전에
그런 조각이 없어 **아바타가 가만히 서 있는다.**

같은 이유로 두 곳을 더 고쳤다.
- `qaAgent` — 답변을 글로스로 바꾸는 데 규칙 기반을 쓰고 있었다(질문 탭·콘솔에 보이는 글로스).
- `AgentConsole` — 소개 페이지에서 **심사위원이 보는** 파이프라인 출력이 규칙 기반이었다.

바꾼 뒤 콘솔 출력이 달라졌다:

    전: (조사만 뗀 한국어 낱말들)
    후: 비내리다1 · 경보 · 주의보1 · 하천변 · 저지대 · 침수0 · 위험 · 즉시0 · 대피0
        Q&A 답변 → 서울1 · 현재 · 위치0 · 관악구 · 신림동 · 기준0  (지역이 앞으로 = 수어 어순)

문서와 코드가 어긋나 있으면 **문서가 맞다고 착각하고 넘어간다.** 문서에 3단이라고
적어 둔 덕분에 찾을 수 있었다 — 적어 두는 일이 이렇게 돌아온다.

## 느린 기기에서 수어가 슬로모션이 되던 문제 (2026-08-17 21:2x)

키오스크를 하루 종일 켜 두는 상황을 가정해 6분 소크 테스트를 돌렸다. 힙은 44MB로
평평했고(누수 없음), 대신 **다른 것이 걸렸다**: 재난문자 하나가 140초가 지나도 끝나지
않았다. 622프레임이면 30fps에서 20.7초여야 한다.

원인은 재생 루프가 **requestAnimationFrame 한 틱에 한 프레임씩만** 넘긴 것이었다.
화면이 느린 기기에서는 틱이 초당 몇 번밖에 안 오므로, 프레임을 못 따라가는 게 아니라
**재생 자체가 늘어난다.** 소프트웨어 렌더링 환경에서 20초짜리가 5분이 됐다.

이건 성능 문제로만 볼 수 없다. **수어는 속도가 뜻의 일부다** — 같은 동작도 빠르면
급한 뜻이고 느리면 천천히라는 뜻이다. 늘어진 수어는 틀린 수어다.

지난 시간만큼 프레임을 건너뛰도록 고쳤다(한 번에 최대 6프레임 — 탭 전환 등으로 길게
멈췄다 돌아온 경우 동작이 튀지 않게). 같은 환경에서 300초 → 52초가 됐고, 렌더가
따라오는 기기에서는 정확히 실제 속도로 재생된다.

저사양 폰·키오스크에서 이 차이가 드러난다. 개발 기기에서만 보면 영영 못 보는 종류다.

## 행동요령을 표현 가능한 말로 다시 썼다 — 91.6% → 100% (2026-08-17 22:4x)

재난별 행동요령은 **사용자가 위급할 때 실제로 보는 문장**이다. 그런데 표현률이 91.6%
였고, 더 나쁜 것은 몇 문장이 **틀리게** 번역되고 있었다:

    "전기와 가스를 차단하세요"       → 전기 가스 **ASF** 멈추다   (차단 → 가축질병 용어)
    "외출을 자제하고 실내에 머무르세요" → 밖 **환기** 하지마
    "밖으로 나온 뒤 다시 들어가지 마세요" → 밖 다시 **들다 가지다** 하지마

행안부 요령의 **뜻은 그대로 두고 낱말만** 동작 사전에 있는 것으로 바꿨다.

    탁자 아래로 → 가구 아래로 · 흔들림이 멈추면 → 흔들리는 것이 멈추면
    차단하세요 → 잠그세요 · 실내에 머무르세요 → 안에 있으세요
    빙판길 → 얼음길 · 전열기 플러그 → 난방 기계 전기 · 노약자 → 노인과 아이

여기에 관형형(나온·갈·볼)을 활용형 생성에 추가해 **100.0%**가 됐다.
문장을 바꾸는 것이 사전을 늘리는 것보다 정확한 경우가 있다 — 특히 **원문을 우리가
쓰는 문장**이라면. 재난문자처럼 남이 쓴 문장은 그럴 수 없다.

## 사용자가 처음 넣는 말이 가장 안 됐다 (2026-08-18 00:0x)

사용자가 "수어 번역이 제대로 안 되는 것 같다"고 했다. 재 보니 그대로였다.

    안녕하세요        → (없음)
    화장실이 어디예요?  → (없음)
    얼마예요?         → (없음)
    도와주세요        → 도        ← 도(도수)로 오역
    밥 먹었어요?      → 먹다      ← '밥' 사라짐

밤새 재난문자 95.8%, 행동요령 100%, 창구 86.3%를 만들어 놓고, **사람이 앱을 켜고
처음 치는 말**은 하나도 제대로 못 하고 있었다. 측정 집합이 전부 격식체 창구·재난문이라
이 부류가 시야에 아예 없었다.

원인은 다섯 갈래였고 모두 구조적이었다.

1. **`-예요/이에요` 계열 어미가 조사 목록에 없었다** — 어디예요·얼마예요·뭐예요가 통째로.
   구어체 종결(이야·인가요·죠·군요·잖아요…)을 파이썬·TS 양쪽에 같이 넣었다.
2. **한 글자 명사가 단독으로 나오면 버려졌다** — 밥·물·손·돈·눈처럼 생활에서 가장 자주
   쓰는 말이 하필 한 글자다. 지문자 자모('가')가 끌려 나오는 것이 무서워 1글자 조회를
   막아 두었는데, 그 대가가 너무 컸다. **사람이 확인한 목록(ONE_CHAR_NOUNS)만** 키로
   넣어 둘 다 해결했다.
3. **안녕·화장실·인사·도움이 웹 사전에서 빠져 있었다**(전체 사전에는 있었다).
4. **ㅂ 불규칙이 모음조화를 안 지켰다** — 돕다 → '도워'로 만들어 '도와'가 사전에 없었고,
   그 자리를 명사 '도' + 조사 '와'가 차지해 "도와주세요"가 '도'가 됐다.
5. **명사 + 조사가 동사 활용형을 덮고 있었다.** 용언을 먼저 세우고, 명사가 덮는 것은
   **격조사(은·는·이·가·을·를)일 때만** 허용했다 — '도와'는 동사가, '입을'은 명사가 맞다.

결과: 일상회화 40문장 표현률 **95.4%**(완전 표현 37/40). 다른 네 집합은 그대로.
`scripts/audit_sets/일상회화.json`으로 상시 검사에 넣었다.

**교훈은 측정 집합이 곧 시야라는 것이다.** 잘 재고 있다고 믿는 동안, 재지 않는 자리에서
가장 기본적인 것이 무너져 있었다. 사용자가 처음 넣을 말부터 측정 집합에 넣어야 한다.

---

## 2026-08-18 — 수어로 묻고, 위치로 답한다 (`묻기` 화면)

지금까지 이 앱에서 농인은 정보를 **받기만** 했다. 재난문자를 수어로 보고, 창구 직원의
말을 수어로 봤다. 정작 급할 때 필요한 것은 **묻는 쪽**이다. "대피소 어디예요?"는
들을 수 있는 사람이면 지나가는 사람에게 물어 3초에 끝내는 일이고, 농인에게는 그 3초가
통째로 막혀 있다.

```
카메라 앞에서 수어 → 낱말 인식 → 의도 파악 → 내 위치로 답 생성
                                  → 아바타가 수어로 답 + 방향 지도
```

### 판단 1 — 화면 문법을 **영상통화**로

농인에게 영상통화는 이미 배운 대화 방식이다(수어 통역 서비스가 그 형태다). 사용법을
새로 가르치지 않아도 "카메라 보고 수어하면 상대가 수어로 답한다"가 그대로 통한다.
그래서 묻는 동안에는 카메라가 화면 전체, 답하는 동안에는 아바타가 전체이고 카메라는
구석으로 작아진다. 시작은 큰 원형 버튼 하나(📹)다.

### 판단 2 — 후보까지 보고 의도를 잡는다

모델의 1순위 정답률은 0.7863, 5순위 안에 들 확률은 0.9188이다(실측, 처음 보는 수어자
49,721표본). 1순위만 보면 **다섯 번에 한 번은 의도를 놓친다.** 낱말별 상위 후보를 함께
보되 순위가 낮을수록 가중치를 낮춰(1 · 0.5 · 0.3 · 0.2 · 0.15) 합산하고, 1.3을 넘을
때만 판정한다. "병원"이 3순위로 밀려도 "어디"가 확실하면 건진다(0.3+1.0=1.3).
반대로 둘 다 2순위뿐이면(0.5+0.5) 판정하지 않는다.

**못 미치면 "모르겠어요"라고 답한다.** 억지로 하나를 고르면 엉뚱한 대피소를 알려주게
되고, 사용자는 그쪽으로 걸어간다. 재난 상황에서 그것은 침묵보다 나쁘다.
`scripts/intent_cases.json`(27건)에 오검출 사례를 절반 가까이 넣은 이유다.

### 판단 3 — 지도 타일을 쓰지 않는다

재난 때 가장 먼저 끊기는 것이 통신이다. 타일을 받아 그리는 지도는 **정작 필요한 순간에
회색 화면**이 된다. 그래서 내 위치를 한가운데 두고 주변 장소를 상대 위치로 직접 그린다
(`DirectionMap`). 데이터가 이미 기기에 있으므로 비행기 모드에서도 그대로 그려진다.
나침반이 있으면 화면을 내가 보는 쪽 기준으로 돌린다 — 북쪽 고정 지도는 머릿속에서
한 번 변환해야 읽히지만, 화면이 돌면 화살표 쪽으로 그냥 걸으면 된다.
골목까지 필요하면 '지도 앱으로 열기'가 있다(그때는 통신이 필요하다).

길 모양은 그리지 않는다 — 도로 데이터가 없다. **없는 것을 그린 척하지 않는다.**

### 판단 4 — 목록의 출처를 화면이 밝힌다

현재 실린 목록은 OpenStreetMap에서 받은 3,888곳(수도권)이고 **공식 지정 대피소 목록이
아니다.** `nearby.json`이 `official: false`를 들고 있고 화면이 그대로 "참고용"이라고
쓴다. 공공데이터포털 키가 있으면
`python3 ml/tools/build_nearby.py --gov-key <키>`로 행정안전부 표준데이터를 받아
교체하면 `official: true`가 되고 경고 문구가 사라진다.

대피소를 사실과 다르게 말하는 것은 정보가 없는 것보다 나쁘다. 농인은 그것을 믿기
때문이다 — 오역에 대해 이미 내린 판단과 같은 기준이다.

### 발견한 오역 — 북동쪽 → **km 지진 크기**

답변 문장을 실제로 돌려 보니 `북동쪽`이 통째로 엉뚱한 글로스가 됐다. 세 글자 방위어는
복합어 프루닝 기준(4글자 이상)에 걸리지 않아 통계 잡음이 그대로 1순위로 남아 있었다.
대피 방향을 틀리게 말하는 것은 말하지 않는 것보다 나쁘므로 8방위·거리 단위를 사람이
확정한 시드로 넣었고, 회귀 사례 6건(`never: km, 지진`)으로 못 박았다.

### 잰 것

| 검사 | 결과 |
|---|---|
| 의도 판정 `scripts/check_intent.mjs` | 27/27 (오검출 0) |
| 길찾기 계산 `scripts/check_nearby.mjs` | 거리·방위·어림수·답변문장 전부 통과 |
| 길찾기 답변 표현률 `audit_sets/길찾기.json` | 28문장 **100.0%** |
| 기존 네 집합 | 재난 95.8 · 행동요령 100 · 창구 86.3 · 홀드아웃 82.8 (변화 없음) |
| 일상회화 | 95.4 (변화 없음) |

인식→의도→답 계산은 **e2e가 아니라 위 두 스크립트로 잰다.** 가짜 카메라는 빈 영상이라
낱말이 나오지 않아, 화면 검사로는 이 경로를 잴 수 없다. e2e는 입구가 살아 있는지와
장소 목록이 실려 있는지만 본다.

---

## 2026-08-18 — 표현률은 그대로인데 번역이 나아진 날 (형태론 > 공기 통계)

`북동쪽 → km 지진 크기`를 잡다가, 같은 모양의 오역이 얼마나 더 있는지 알 방법이
없다는 것을 깨달았다. 표현률은 이런 오역을 **성공으로 센다** — 수어가 나가긴 나갔으니.
그래서 검토 도구를 먼저 만들었다.

```bash
python3 ml/tools/suspect_align.py --top 60
```

의심할 이유가 있는 것만 좁힌다: 낱말과 글로스가 **글자를 하나도 공유하지 않고**,
그런데도 **말뭉치에 자주 나오고**, 사전의 다른 조각으로 **분해되지도 않는** 것.
175,843개 중 1,002개가 걸렸다. 전부 오역은 아니다(대피→도망, 한파→춥다는 맞다).
그래서 자동으로 지우지 않고 **목록만** 내고 사람이 본다.

빈도 상위에서 세 갈래의 구조적 오역이 나왔다.

### ① 순수 문법 어미가 공기 통계에 먹혔다 — 재난문자에서 가장 흔한 말투

| 낱말 | 실제로 나오던 수어 | 고친 뒤 |
|---|---|---|
| 있겠습니다 | 소나기 | 있다 |
| 많겠습니다 | 구름 | 많다 |
| 오겠습니다 | 구름 | 오다 |
| 받겠습니다 | 고기압 | 받다 |
| 되겠습니다 | 마이너스 | 되다 |
| 춥겠으니 | 몸 | 춥다 |
| 낮겠습니다 | 보다 | 낮다 |

"많겠습니다"의 뜻은 '많다'다. 다른 무엇도 아니다. 그런데 날씨 문장에서 이 말이
'구름'과 늘 함께 나오니 Dice가 **많겠습니다 → 구름**을 1순위로 올렸다. 이 부류는
빈도가 아무리 높아도 통계를 믿을 이유가 없어, `-겠습니다·-습니다·-세요` 같은
**순수 문법 어미로 끝나는 표면형은 빈도와 무관하게 어간의 글로스**를 쓰게 했다.
명사가 이 꼴로 끝나는 일이 없어 다른 낱말과 부딪히지도 않는다.

### ② 관측이 얇은 활용형은 형태론이 이긴다

없어→공식, 같은→동광우방, 아닌→판단, 크게→차이, 높은→평균, 더운→격차,
어려운→힘들다, 가까운→한적있다… 110여 개가 제자리를 찾았다.

기준값(`--conj-trust`)은 추정하지 않았다. 0·20·50·200·400·800·1500으로 돌려
**바뀌는 매핑을 한 줄씩 눈으로 보고** 정했다. 그 과정에서 예외 넷이 필요하다는 것이
드러났고, 넷 다 실제 퇴행을 보고 넣은 것이다:

1. 표면형이 **그 자체로 표제어**면 두지 않으면 사고→사다, 신고→신다가 된다.
2. **표제어에서 직접 만든 키**(아니다→'아니')도 마찬가지 — 아니→알다가 됐다.
3. **이미 다른 용언이 가져간 자리**는 두지 않으면 드문 쪽이 나중에 훔쳐 간다 —
   떨어집니다(떨어지다)를 '떨다'가, 마시고(마시다)를 '말다'가 가져갔다.
4. **-어지다 꼴**은 별개의 낱말이다(넘다 ≠ 넘어지다). 덮으면 뜻이 바뀐다.
   대신 -어지다 표제어가 마지막에 자기 활용형을 되찾는다.

### ③ 조사만으로 된 키가 사전에 남아 있었다

**에서 → IC**. 조사가 덜 떨어진 토큰이 여기 걸리면 문장 한가운데 엉뚱한 수어가
들어간다. 조사 전용 키 7개(에서·까지·부터·으로·처럼·만큼·이나)를 지웠다.

그리고 사람이 확정한 시드 8개: 매우→심하다(WHO였다), 확산→퍼지다, 착용→입다
(마스크였다 — 착용은 쓰는 행위지 마스크가 아니다), 한때→가끔, 관리→돌보다,
없도록→없다, 주시고→주다.

### 잰 것 — **표현률은 오히려 0.2%p 내려갔다**

| 집합 | 전 | 후 |
|---|---|---|
| 재난문자 246 | 95.8% | **95.6%** |
| 창구 58 · 홀드아웃 37 | 86.3 · 82.8 | 86.3 · 82.8 |
| 행동요령 38 · 길찾기 28 | 100 · 100 | 100 · 100 |
| 일상회화 40 | 95.4 | 95.4 |
| 오역 회귀 | 40건 | **55건 통과** |

내려간 0.2%p는 **조사(부터·까지)를 지운 만큼**이다. 예전에는 `에서 → IC`처럼
아무 뜻 없는 동작이 나가면서 "표현됨"으로 세어졌다. 그걸 지웠으니 숫자가 내려간다.

**이 숫자가 이 프로젝트에서 표현률을 그대로 믿으면 안 되는 이유를 그대로 보여준다.**
번역은 분명히 나아졌는데(오역 30건 이상이 사라졌다) 지표는 내려갔다. 그래서
`suspect_align.py`와 회귀 사례(55건)를 함께 남긴다 — 표현률은 "얼마나 나갔나"를,
저 둘은 "맞게 나갔나"를 잰다.

### 이어서 — 도구가 알려 준 두 갈래 더

`suspect_align.py` 상위 100개를 마저 보니 두 갈래가 더 나왔다.

**행정단위·시점을 뗀 어간이 사전에 있는데 통짜 키의 통계가 이겼다.**
부산시→회의, 서울시→대곡, 광명시→노온사동, 전주시→완산, 제주시→한북로.
같은 모양으로 **-시(때)** 도 무너져 있었다 — 발열시→463, 접촉시→짧다,
증상시→발열, 식사시→대화, 확인시→무주. 어간이 그대로 표제어면 그것이 답이다
(부산시는 부산이고, 발열시는 발열이다). 32개가 제자리를 찾았다.

`면`과 `도`는 접미사 목록에서 뺐다 — 용언 어미(-면)·조사(-도)와 부딪혀
**가시면 → '가시'(가시나무)** 가 된다. 규칙을 넓히기 전에 실제 목록을 보고 잘랐다.
`장흥군`처럼 통짜 이름이 그 자체로 동작 사전에 있는 경우도 건드리지 않는다.

**재난문자 상투구가 사전에 남아 있었다.** 입니다→mm, 것으로→오래, 등은→캠프,
내에서→금호동. 문장 한가운데 아무 뜻 없는 동작이 들어간다. 파이썬·TS 양쪽
STOP_WORDS에 함께 넣었다(한쪽만 고치면 사전 키가 어긋나 조용히 못 찾는다).

최종: 재난문자 **95.7%**(95.8에서 0.1%p 내려감 — 지운 조사·상투구만큼),
나머지 다섯 집합은 그대로, 오역 회귀 **65건 통과**(40 → 65로 늘렸다).

### 3차 — 관형형 -는과 ㅂ 불규칙 활용이 통째로 빠져 있었다

`suspect_align.py` 상위 130까지 마저 보니 두 구멍이 더 있었다.

**관형형 `-는`이 활용형 목록에 없었다.** 받는·먹는·없는·맞는·넘는·말하는·원하는…
한국어에서 가장 흔한 꼴 중 하나인데 통째로. 그 자리를 통계 잡음이 채우고 있었다:
**받는 → 영동**, **먹는 → 많이먹다**, **없는 → 없다7**, **맞는 → 가볍다**,
**넘는 → 방파제**.

**ㅂ 불규칙이 `-아/어`만 만들고 `-우니·-우며·-우세요·-웠어요`를 안 만들었다.**
그래서 **미끄러우니 → 익숙하다**. 빙판길 안내 문장에서 이 말이 빠지면
"길이 ㅇㅇ 조심하세요"가 된다.

여기에 시드 14개를 더했다(권고→WHO였다, 야외→행동, 현장→해결, 내집→빗자루,
단독주택→혼자, 당부드립니다→계량기, 영향권→**바비**(태풍 이름이 낱말 자리에 있었다)).

결과: 사전이 175,843 → **182,290개**, 오역 회귀 **74건 통과**.
그리고 **창구 홀드아웃이 82.8% → 83.6%로 올랐다** — 튜닝에 쓰지 않은 집합이라
이 0.8%p가 일반화가 실제로 늘었다는 뜻이다. 나머지 집합은 그대로.

### 4차 — 창구 대화가 86.3 → 92.3, 홀드아웃이 82.8 → 89.7

창구 집합에서 빠지던 낱말을 어간까지 따라가 보니, 없는 것이 아니라 **꼴이 없었다.**

- **명사형 `-기`** 가 활용형 목록에 없었다 — 숨쉬기·걷기·기다리기.
  `숨쉬다0` 클립이 있는데도 "숨쉬기 힘드신가요"가 통째로 빠지고 있었다.
- **ㄹ 불규칙에 존대·의문 꼴이 없었다** — 힘드신가요·드셨어요·모실까요.
  `-세요/-십시오/-ㅂ니다`만 만들고 있었다.
- **웹 동작 사전에서 잘려 있던 것들** — 남다·비우다·돌다·느리다·맛·숨쉬다·졸리다.
  전체 사전에는 있는데 빈도로 잘렸다. 생활 어휘 목록에 넣었다.

그리고 **뜻이 뒤집히던 두 가지**를 사람이 확정했다.

- **숨 → 숨다(몸을 숨기다)**. "숨을 크게 들이쉬어 보세요"가 '숨다'로 나가면
  뜻이 정반대다. 숨(호흡)은 클립이 없어 `숨쉬다0`으로 잇는다.
- **드세요 → 들다(들어올리다)**. 병원·약국·식당에서 '드시다'는 언제나 먹는 쪽이다.
  드세요·드시면·드셨·드십니다를 `먹다`로 확정했다.
  ("가방을 드세요"가 먹다가 되지만, 이 앱이 쓰이는 자리에서는 훨씬 드물다.)

없는 낱말은 **뜻이 가장 가까운 있는 낱말**로 이었다 — 복용→먹다, 피검사→검사,
식후→식사, 천천히→느리다, 매운/맛있게→맛. 정확히 같지는 않지만 화제는 살아
남는다. 아무것도 안 나가는 것보다 낫다는 판단이다(대피 방향처럼 **틀리면
위험한** 자리에는 이 방식을 쓰지 않는다 — 거기는 사람이 확정한 것만 쓴다).

| 집합 | 어제 | 오늘 |
|---|---|---|
| 창구 대화 58 | 86.3% | **92.3%** |
| **창구 홀드아웃 37** | 82.8% | **89.7%** |
| 일상회화 40 | 95.4% | **96.9%** |
| 재난문자 246 | 95.8% | 95.8% |
| 행동요령 38 · 길찾기 28 | 100 · 100 | 100 · 100 |
| 오역 회귀 | 34건 | **82건 통과** |

홀드아웃 +6.9%p가 오늘의 가장 정직한 숫자다 — 고칠 때 보지 않은 집합이다.

### 5차 — 지명·태풍 이름이 낱말 자리에 들어와 있었다

`suspect_align.py` 상위 200까지 보니, 가장 위험한 부류가 따로 있었다.
**고유명사가 일반 낱말의 뜻으로 쓰이고 있었다.**

| 낱말 | 실제로 나오던 수어 | 무엇인가 |
|---|---|---|
| 폭우 | **하이선** | 2020년 태풍 이름 |
| 삼가 | **양재** | 서울 지명 |
| 자택 | **권선** | 수원 지명 |
| 주택 · 농경지 | **읍면** | 행정단위 |
| 국제학교 | **TCS** | 학교 약칭 |
| 무궁화호 | **꽃** | 열차 이름을 꽃으로 |
| 서울특별시청 | **더럽다** | — |

재난문자 말뭉치에서 "폭우"와 "하이선"이 같은 문장에 자주 나왔기 때문이다. 통계로는
자연스러운 결과지만, **"폭우가 내립니다"가 수어로 "하이선"이 되면 읽는 사람은
무슨 말인지 알 수 없다.** 25개를 사람이 확정했다(빙판→미끄럽다, 강추위→춥다,
긴급→급하다, 낙상→넘어지다, 구매→사다, 실내→안…).

문장 상투구 여섯 개(있으며·예정이니·완료되었으며·기하시기…)도 STOP_WORDS에 넣었다.

재난문자 **95.9%**(+0.1%p), 오역 회귀 **92건 통과**. 나머지 집합은 그대로.
일상회화 홀드아웃은 손대지 않았고 **87.9%** 그대로다.

### 넓은 화면에서 아바타가 절반을 비워 두고 작게 있었다

태블릿 가로·키오스크의 좌우 2단에서, 아바타와 대화 기록이 `flex-1`로 자리를
반씩 나눠 갖고 있었다. 기록이 한 줄뿐인데도 화면 절반을 비워 두고 아바타는
작게 남았다(사진으로 확인). 넓은 화면에서만 3:1로 바꿨다.


---

## 2026-08-18 — 오프라인이라고 적어 두고 반쪽이었다 (MediaPipe 동봉)

`묻기` 화면을 만들며 "재난 때가 곧 오프라인이라 타일 지도를 쓰지 않는다"고 적었다.
답을 만드는 쪽(장소 목록·거리 계산·번역 사전·아바타 조각)은 사실이었다.

**인식 쪽은 아니었다.** `src/recognition/holistic.ts`가 MediaPipe 런타임과
`.task` 모델을 jsdelivr CDN과 Google 저장소에서 받아 오고 있었다. 회선이 끊기면
**카메라가 아예 켜지지 않는다** — 물어볼 수가 없으니 답도 없다. 정작 그때가
"대피소 어디?"를 물어야 하는 때다.

배포 준비를 하다 요청 로그를 보고서야 알았다. 화면으로는 보이지 않는다 —
인터넷이 있는 곳에서는 잘 되니까.

그래서 32MB(런타임 19MB + 모델 14MB)를 동봉했다. 없으면 기능 자체가 없는 것이라
용량을 내주는 편이 맞다. CDN은 폴백으로만 남긴다(동봉 파일이 빠진 채 배포된 경우).

곁가지로 둘이 더 나왔다.

- **번들러가 만들어 놓고 아무도 안 쓰는 ORT wasm 13.5MB.** `onnxRecognizer`가
  `wasmPaths`를 `public/ort/`로 고정하기 때문에 `assets/` 쪽 사본은 한 번도
  요청되지 않는다(실측: 요청 로그 0회 / `ort/` 쪽 8회). 빌드에서 지운다.
- **`public/reference_demo.html` 3.9MB** — 코드 어디서도 참조하지 않는다. 지웠다.

e2e에 두 가지를 넣어 다시는 조용히 CDN으로 돌아가지 못하게 했다:
동봉 파일이 실제로 서빙되는지(HEAD 200), 그리고 **폴백 경고가 안 뜨는지**.
폴백은 예외가 아니라 `console.warn`이라 오류 검사에 걸리지 않는다 —
경고를 따로 모아서 본다.

### 군살을 지웠더니 오프라인이 통째로 죽었다 (빌드 단계 순서)

안 쓰는 ORT wasm 13.5MB를 지우는 정리를 `build-offline-manifest`에 넣었다.
그런데 그 **앞 단계**인 `inject-sw-precache`가 이미 그 파일을 서비스워커
프리캐시 목록(CORE)에 넣어 둔 뒤였다.

    tsc → vite build → inject-sw-precache(목록 작성) → build-offline-manifest(파일 삭제)

없는 파일을 `cache.addAll`이 받으러 가면 **install이 통째로 실패한다.** 서비스워커가
활성화되지 않으니 오프라인이 전부 죽는다 — 그런데 **온라인에서는 아무 증상이 없다.**
화면도 멀쩡하고 콘솔 오류도 안 난다.

e2e의 오프라인 판이 `ERR_INTERNET_DISCONNECTED`로 잡았다. 이 검사가 없었으면
고정 주소로 옮긴 뒤 "왜 비행기 모드에서 안 되지"를 한참 찾았을 것이다.

목록을 만드는 쪽에서 제외하도록 고쳤다(순서에 기대지 않는다).
**빌드 단계가 서로의 산출물을 건드릴 때는 순서를 가정하지 말 것.**

---

## 2026-08-18 — 시각·날짜를 사람이 쓰는 꼴로 (전용 동작 + 시간대 표지)

"수어가 자연스럽지 않다"를 파고들다 가장 흔한 자리에서 어긋난 것을 찾았다.

사람 번역가는 "21시"를 **`시:9시`**, "10.26(화)"를 **`날짜:10월26일 화요일1`**이라는
전용 동작으로 낸다. 말뭉치에서 `시:*`가 **17,149회**, `날짜:*`가 **6,640회** —
가장 많이 쓰는 부류 중 하나다. 우리는 이 조각을 **웹 사전에서 통째로 빼 놓고**
숫자를 자릿수로 읽고 있었다("이 십 일 시"). 형식도 어법도 틀린 셈이다.

뺀 이유는 있었다 — `:` 글로스가 번역 사전에 들어가면 "주택 → 날짜:9월16일" 같은
오염이 생긴다(실측에서 낱말 2만 개가 그 상태였다). 그런데 **조각을 싣는 것과
낱말로 쓰는 것은 다른 문제였다.** 조각은 싣되, 앱이 문장에서 시각·날짜 패턴을
알아봤을 때만 표(`timegloss.json`)를 보고 꺼내 쓰게 했다. 번역 사전은 예전처럼
`:` 글로스를 제외하므로 오염은 구조적으로 생기지 않는다.

### 시간대 표지 — 이게 진짜 중요했다

수어 시각은 **12시간제**다. `시:9시`만으로는 **오전 9시인지 21시인지 알 수 없다.**
그래서 사람 번역가는 앞에 시간대를 붙인다(아침 6,120회 · 새벽 4,259 · 밤 3,331 ·
저녁 2,865 · 오후 2,310 · 낮 1,449).

어느 시각에 어느 표지가 붙는지 **원문 시각별로 26,000여 건에서 쟀다**:

| 원문 | 표지 | 일치율 |
|---|---|---|
| 0시 | 밤 | 70% |
| 1–6시 | 새벽 | 79–92% |
| 7–11시 | 아침 | 73–94% |
| 12시 | 낮 | 92% |
| 15–16시 | 오후 | 62–64% |
| 18–20시 | 저녁 | 54–87% |
| 21–23시 | 밤 | 96–100% |

추정이 아니라 잰 값이다. 13·14·17시만 두 표지가 팽팽해(오후/낮 44:33, 낮/오후
40:37, 저녁/오후 54:43) 순서가 자연스러운 쪽을 골랐고, 그 사실을 코드에 적어 뒀다.

원문이 "오후 3시"처럼 이미 시간대를 말했으면 그 말을 그대로 쓴다.

### 범위 물결표

"11:00~15:30", "1.7(목)~1.10(일)" — 사람 번역가는 `물결표1`을 쓴다(말뭉치 282회,
앞 글로스가 `시:*` 49회 · `날짜:*` 45회). 넣었다. 다만 **어순 재배치에서 떨어져
나가면 뜻이 사라진다** — 실측에서 "날짜 날짜 물결표"로 맨 뒤에 밀렸다. 앞 덩어리에
묶어 고정했다.

### 결과 (사람 번역과 나란히)

```
원문   1.7(목)~1.10(일)까지 임시선별검사소 운영
사람   날짜:1월7일 목요일1 물결표1 날짜:1월10일 일요일1 까지1 임시1 …
지금   날짜:1월7일 목요일1 물결표1 날짜:1월10일 일요일0 임시1 …

원문   오늘 21시부로 한파가 예상되오니
사람   오늘1 밤1 시:9시 시작1 갑자기1 춥다1 오다1 …
지금   오늘 밤1 시:9시 한파 예상0 대비0
```

### 곁가지 — 과거형 '-했'이 활용형 목록에 없었다

"발생했습니다"는 어미를 떼면 '발생했'이 되는데 그 키가 없어 통계 잡음이 자리를
차지했다 — **발생했 → 광산**(지명). 재난문자에서 가장 흔한 서술 꼴 중 하나다.
`-했/-했다/-했어요/-했습니다/-했는데/-했으니`를 명사 파생형에 넣었다.

### 지표가 한 번 내려갔다가 올라왔다

`21시부터`가 시각 동작 하나로 나가면서 `부터`가 낱말로 남았다. 감사 도구는 두 글자
이상 한글을 전부 내용어로 세므로 이게 "빠진 낱말"로 잡혀 재난문자가 95.9 → 95.4%로
**내려갔다.** 번역은 분명히 나아졌는데. 조사를 STOP_WORDS에 넣어 정리하니(범위는
물결표로 표시하므로 조사 자체는 옮기지 않는다) **96.1%**로 올라왔다.

| 집합 | 전 | 후 |
|---|---|---|
| 재난문자 246 | 95.9% | **96.1%** |
| 창구 58 · 홀드아웃 37 | 92.3 · 89.7 | 그대로 |
| 일상회화 40 · 2차 50 · 홀드아웃 50 | 96.9 · 95.4 · 87.9 | 그대로 |
| 오역 회귀 | 102건 | **114건 통과** |

**지표가 번역 품질과 어긋나는 자리를 오늘만 세 번 봤다.** 표현률은 "낱말이 나갔나"만
잰다 — 자연스러움도, 뜻이 전달됐는지도 재지 않는다. 그 둘은 아직 **아무도 재고
있지 않다.** 당사자 평가가 필요한 이유다.

---

## 2026-08-18 — 학습 번역 모델은 **재난문자에서만** 쓴다

번역을 통계 사전에서 학습 모델로 바꾸고 배포까지 했는데, 배포 뒤에 실제 문장을
넣어 보고서야 알았다. **모델이 학습 도메인 밖에서 무너진다.**

```
오늘 21시부로 한파가…   → 오늘 밤 시:9시 시작 춥다 올것이다 혹시 준비   ✓
화장실이 어디예요       → 꽃 꽃 꽃 꽃 … (47개)                        ✗
도와주세요             → 지역 금요일 금요일 … (47개)                   ✗
어디가 아프신가요       → <unk> × 17                                   ✗
```

**내 검증이 부족했다.** BLEU·F1을 **재난문자 검증 split에서만** 쟀다. 창구·일상
감사 세트로는 재지 않고 "사전보다 3배 낫다"며 모든 번역의 기본값으로 만들었다.
창구 대화가 이 앱의 주 용도인데도.

### 두 갈래로 대응했다

**① 반복 고리 차단** — 그리디 디코딩이 같은 낱말을 끝없이 내는 상태에 빠진다.
막는 기준은 **사람 정답에서 쟀다**(40,002문장):

| 최대 연속 반복 | 1회 | 2회 | 3회 이상 |
|---|---|---|---|
| 비율 | 62.6% | 36.2% | **1.3%** |

2연속은 흔하고(강조·열거) 3연속부터는 사실상 없다. 그래서 **3연속만** 막고,
같은 3낱말 묶음이 되풀이되는 것도 막는다. 결과:

| 집합 | 전 | 후 |
|---|---|---|
| 재난문자 | 31% 무너짐 | **2%** |
| 길찾기 | 18% | **0%** |

**② 도메인 분기** — 이게 본질이다. 반복을 막아도 창구·일상 출력은 **무너지지
않고 그냥 틀린다**(꽃 꽃 지도 지시# 물 준비 가능 높다). 모양으로는 거를 수 없다.

    받기(재난문자)·행동요령  →  학습 모델 (글로스 F1 18.7 → 55.8)
    창구 대화·자유 입력·묻기 답변  →  통계 사전 (표현률 92.3% · 96.9%)

기본값은 `everyday`(사전)로 두었다 — **모르는 자리에서는 덜 틀리는 쪽**을 쓴다.

### 배운 것

"사전보다 3배 낫다"는 맞는 말이었지만 **어디서 3배인지**를 빼먹었다. 도메인이
좁은 데이터로 학습한 모델은 그 밖에서 조용히 자신 있게 틀린다 — 이 앱에서는
농인이 그것을 믿는다는 뜻이라 특히 위험하다.

**앞으로 모델을 붙일 때는 학습 도메인 밖 집합으로 먼저 잰다.** 감사 세트가
여섯 개나 있었는데도 쓰지 않은 것이 실수였다.

일상·창구 번역쌍이 생기면(수어영상 SEN 데이터에 한국어 원문이 붙는다면) 그때
같은 모델을 그 자리까지 넓힐 수 있다.

## 지문자 데이터는 **있었다** — 앞선 판단을 뒤집는다 (2026-08-18)

이 문서 위쪽(2026-08-17)에 "지문자 데이터(FINSP)는 수어영상(103) 목록에 없음을
확인"이라고 적어 두었다. **틀렸다.** 목록에 `FINSP`라는 이름이 없었을 뿐,
지문자는 CROWD 조각 안에 들어 있다.

어떻게 놓쳤나 — 이 프로젝트에서 두 번째로 밟은 같은 함정이다.

| | 가이드 표기 | 실데이터 표기 |
|---|---|---|
| 단어 | `WRD` | `WORD` |
| **지문자** | **`FINSP`** | **`FS`** |

`ml/signbridge/naming.py`의 갈래 정규식이 `FINSP|FSP`만 받았다. 실제 파일은
`NIA_SL_FS0001_CROWD01_F`다. 그래서 갈래 필터를 통과하는 클립이 **0개**였는데
오류도 경고도 나지 않았다 — 그냥 조용히 0건이었다. 목록에서 이름을 못 찾은 것도
같은 이유다. `FS`를 별칭으로 추가했다.

실제로 무엇이 있나 (CROWD 형태소 조각 8MB를 열어 직접 셈, `ml/tools/fs_report.py`)

| 항목 | 실측 |
|---|---|
| 지문자 클립 | **17,000개** |
| 낱말 | **1,015종** (서울 지명·거리 이름 위주. 양끝 공백이 붙은 라벨 8건은 정리 — 안 하면 1,022종으로 갈린다) |
| 자모 | **37종 · 133,986회** (가장 흔한 ㅇ 16,881 · 가장 드문 ㄻ 17) |
| 100회 미만 자모 | ㅋ ㄲ ㄸ ㅆ ㄻ — 여기서 먼저 틀릴 것이다 |
| 낱말당 자모 | 중앙값 8 · 최대 24 |

**정말 자모를 하나씩 쓰는 것인가.** 라벨은 낱말 하나뿐이고 자모별 시각이 없어
직접 확인할 수 없다. 대신 표시 구간 길이를 자모 개수에 견줬다 —
**상관 r = 0.845, 자모 하나에 약 0.42초**(1자모 1.43s → 21자모 9.74s). 낱말을
통째로 흉내 내는 동작이면 길이가 자모 수와 무관해야 한다. 하나씩 쓰는 게 맞다.

**그래서 무엇을 하려는가.** 낱말 통째로 분류하면 배운 1,015개 지명만 알아듣는다.
자모열을 라벨로 두고 CTC를 학습하면 **배우지 않은 이름도 읽는다** — 지문자를 쓰는
이유가 원래 그것이다(사람 이름·지명에는 수어 단어가 없다). `--jamo`로 라벨을
자모열로 펼치게 했고, 자모마다 시각을 지어내지 않는다(CTC는 시각 없이 학습된다).

**아직 못 한 것.** 키포인트(CROWD keypoint 12GB · filekey 39580·39582·39474)를
받으려면 API 키가 필요한데 이 워크스페이스의 `AIHUB_APIKEY`가 비어 있다(작은
파일로 시험해 502 "인증실패" 확인). `ml/jobs/run_fingerspell.sh`에 내려받기부터
자모열 팩 생성까지 묶어 두었다 — **키만 넣으면 한 줄로 돈다.**

**아바타가 지문자를 쓰는 것은 아직 못 한다.** 키포인트를 받아도 자모별 구간이
라벨에 없어서 자모 동작을 잘라낼 수 없다. CTC를 학습한 뒤 강제 정렬로 경계를
얻는 길이 있지만, 해 보기 전에는 된다고 적지 않는다. 지금은 낱말 카드로
정보 손실만 막는 상태 그대로다.

## 겹받침을 풀지 않기로 (2026-08-18)

`ㄻ`을 `ㄹ`+`ㅁ`으로 풀면 자모 종류가 줄어 드문 자모 문제가 완화된다. 풀지 않았다.
실제 지문자에서 겹받침을 두 번 쓰는지 **확인하지 못했기 때문**이다(영상을 받지
않았고 라벨에는 자모별 시각이 없다). 손 모양 하나에 자모 둘을 매기면 CTC 정렬이
어긋난다. 확인 전에는 라벨 표기를 그대로 둔다 — 지어내지 않는 쪽이 낫다.

## 행동요령을 학습 모델에서 사전으로 되돌린다 (2026-08-18)

`onAnswer`에 이렇게 적혀 있었다 — "행동요령은 재난 말뭉치와 같은 말투다,
학습 모델이 배운 자리다." **재 보니 틀렸다.** 학습 모델은 재난문자(공지 말투)
20만 쌍으로 배웠고, 행동요령은 명령 말투다. 배운 적이 없다.

실제 안내 문장 38개를 사전과 모델에 같이 넣어 봤다.

| 원문 | 사전 | 학습 모델 |
|---|---|---|
| 가구 아래로 들어가 몸을 보호하세요 | 가구0 몸 보호 들어가다0 아래 | **지역1 장소1 홍보1** |
| 흔들리는 것이 멈추면 전기와 가스를 잠그세요 | 흔들리다 섞다1 전기0 가스0 잠그다1 멈추다0 | **더1 시작1 온도올라가다1** |
| 엘리베이터를 타지 말고 계단으로 대피하세요 | 엘리베이터1 타다0 계단0 대피0 하지마1 | **주말1 기간1 필요1 때1 사람2#** |
| 지하 공간과 하천 근처를 피하세요 | 지하0 공간0 하천 근처0 피1 | **강1 가깝다1 부상1 지시1#** |

38문장 중 5문장은 모델이 사전의 절반도 못 냈고, 나머지도 대부분 뜻이 어긋났다.
**목숨이 걸린 안내가 헛소리로 나가고 있었다.** 그런데 화면은 멀쩡하다 — 아바타는
무언가를 하고 있고 자막에는 한국어 원문이 그대로 떠 있다. 눈으로는 못 잡는다.

잘못된 수어는 표현되지 않은 것보다 나쁘다(농인은 그것을 믿는다). 행동요령은
`'everyday'`(사전)로 보낸다. 사전은 38문장 전부 글로스를 내고, 그 글로스가 모두
동작 사전에 있다.

`scripts/check_guide_domain.mjs`로 배선과 낱말 덮기를 함께 지킨다.

**재난문자 본문은 그대로 학습 모델을 쓴다.** 거기서는 사람 번역가 정답 기준으로
모델이 사전보다 낫다(BLEU 27.1 vs 18.7). 도메인이 다르면 결과가 다르다는 것을
이번에 다시 확인한 셈이다 — "재난"이라는 한 단어로 묶어 두면 안 된다.

## 통계 사전에 문턱을 걸지 않기로 — 재 보고 접었다 (2026-08-18)

감사에서 오역 몇 개를 잡았다(`급류 → 도청`, `부위 → 뱀1`, `않습니다 → 다스리다1`,
`북상중 → 바비`). 낱개로 시드를 박는 대신 **구조적으로 거를 기준**을 찾아보았다.

**후보 1 — Dice 문턱을 올린다.** 맞는 짝은 dice 0.27~0.85, 틀린 짝은 0.17~0.29라
0.25로 자르면 갈릴 듯했다. 실제로 만들어 재니 재난문자 표현률 96.1 → **90.2%**,
오역 회귀도 **122/124**로 깨졌다(`서울`·`발열`이 사라졌다). 접었다.

**후보 2 — 비율(그 낱말이 나온 문장 중 이 글로스가 같이 나온 비율)로 자른다.**
표본에서는 깨끗이 갈렸다 — 맞는 짝 0.66 이상(폭염→덥다1 0.87, 서울→서울1 0.83),
틀린 짝 0.48 이하(급류→도청 0.21, 부위→뱀1 0.22).

| 문턱 | 재난문자 표현률 | 완전 표현 문장 | 오역 회귀 |
|---|---|---|---|
| 없음 | **96.1%** | 70% | 124/124 |
| 0.40 | 93.7% | 53% | 124/124 |
| 0.55 | 91.4% | 39% | 124/124 |

빈도가 낮은 낱말에만 걸어 보려 했지만 범위가 거의 같았다(22.2% vs 23.6%) —
비율이 낮은 짝은 어차피 대부분 드문 낱말이다.

**그리고 더 중요한 것을 알았다. 비율이 낮다고 틀린 것이 아니다.** 날씨·재난
문장에는 배경처럼 깔리는 글로스가 있다 — `한때`(3,655문장)의 동시출현 1위는
심하다1 44%·지도1 40%인데, 뜻은 `가끔`이다. Dice는 글로스 쪽 빈도로 나누기 때문에
그 배경을 이미 걸러 내고 더 구체적인 글로스를 고른다. **비율로 자르면 Dice가 옳게
해 놓은 일을 되돌리게 된다.**

결론: 문턱은 걸지 않는다(`--min-recall` 기본값 0). **측정된 손실(표현률)과
측정되지 않은 이득(정확도)을 맞바꾸지 않는다.** 잘린 짝 중 얼마가 진짜 오역인지
재려면 사람 검토가 필요하고, 그 검토를 빠르게 하는 쪽에 힘을 쓴다 —
`ml/tools/suspect_align.py`가 비율을 함께 보여 준다. 붙이자마자
`한때 → 가끔1 4%`, `점차 → 구름1 24%`가 눈에 띄었다. 보고 나서 갈렸다 —
`한때 → 가끔1`은 맞는 대응이고(날씨 문장의 '한때'는 '가끔'이다), `점차 → 구름1`은
오역이었다(`점차 흐려지겠습니다`의 배경이 뜻으로 붙었다). **숫자가 골라 주지 않고,
숫자가 좁혀 준 뒤 사람이 고른다** — 이 도구를 그렇게 쓰라는 뜻이다.

낱개 오역 네 건은 시드로 박았다: `않습니다·않는다·않음 → 아니다1`(부정이 빠지면
뜻이 뒤집힌다), `북상중 → 북상0`(태풍 이름 `바비`로 가 있었다), `부위 → 몸`,
`주의·주의사항 → 조심1`, `점차·차차 → 점점1`(날씨 배경 `구름1`·`기압골1`이 붙어 있었다).

## 지명이 낱말 자리를 차지하는 문제 — 감사 3차까지 (2026-08-18)

`suspect_align.py`에 **비율**과 **지명 표시**를 붙이고 세 번 훑어 오역 34건을
시드로 박았다. 표현률은 96.1%로 그대로고(빠진 낱말 114 → 112), 오역 회귀 124건도
계속 통과한다. **커버리지를 잃지 않고 정확도만 올린 셈이다.**

가장 많이 나온 부류가 **지명·기관명 오염**이다.

| 낱말 | 잘못 붙어 있던 글로스 | 고친 것 |
|---|---|---|
| 모든 | **은행1** ("모든 주민" → "은행 주민") | 모두1 |
| 운행하지 않습니다 | **다스리다1** (부정이 사라진다) | 아니다1 |
| 북상중 | **바비** (그해 태풍 이름) | 북상0 |
| 계곡물·급류·조난 | **도청** | (기관명 머리말 제거로 해결) |
| 거주지 | **성북1** | 집1 |
| 세부 | **강동2** | 자세하다1 |
| 전구간 | **신천동로** | 모두1 |
| 자택 | **권선** | 집1 |
| 작업중 | **호계동** | 일하다1 |
| 운항 | **백령도** | 다니다1 |
| 올해 | **손자0** | 년 |

재난문자에는 어느 지역 이야기인지가 늘 붙어 있어서, 지명 글로스가 아무 낱말에나
달라붙는다. **뜻이 완전히 달라지므로 우선순위가 높다.**

도구가 이제 이것을 표시한다. 지명 목록은 지어내지 않았다 — 오늘 찾은 **지문자
라벨 1,015종**(서울 지명·거리 이름)과 행정구역 꼬리로 끝나는 글로스를 쓴다.
낱말 자체가 그 지명을 품고 있으면(`강동구 → 강동2`) 정상이므로 표시하지 않는다.
`로`·`천`은 뺐다 — `앞으로1`·`하천`이 걸려서다.

**시드는 활용형까지 퍼뜨린다.** `중지 → 멈추다0`으로 고쳐 놓고도
"버스 운행이 중지되었습니다"는 여전히 `기차2`였다. 활용형이 저마다 따로 통계를
배우기 때문이다. 표제어만 보고 "고쳤다"고 넘어가기 쉬운 자리다(시드 194개 →
활용형 143개가 더 고쳐졌다).

**되돌릴 뻔한 것 하나.** `-해` 어미를 떼는 규칙을 넣었더니 `올해 → 손자0`이
보여 되돌렸는데, 확인해 보니 **규칙 이전부터 그랬다.** 371문장으로 전후를
대조하니 달라진 문장이 하나뿐이고 그것도 개선이었다. 눈으로 보고 판단했으면
멀쩡한 개선을 버렸을 것이다 — 대조가 살렸다.

## 고개 동작을 늘리지 않고, 물어볼 목록으로 남긴다 (2026-08-18)

지금 아바타가 쓰는 비수지는 고개 끄덕임 9종·흔들기 7종뿐이다. 원본에는 더 있는데
**사람이 확인한 목록(ALLOW_NOD/ALLOW_SHAKE)에 있는 것만** 싣기 때문이다. 늘릴 수
있는지 보려고 ETL을 다시 돌려 통계가 강한데 목록에 없어 빠진 것들을 뽑았다.

| 갈래 | 통계는 강한데 목록에 없어 뺀 것 |
|---|---|
| 흔들기 | 복잡하다(×36) · 일대일(×35) · 지진(×27) · 미달(×22) · 사다(×21) · 마음대로(×20) · 취소(×18) · 활동(×17) · 흔들리다(×16) · 독(×15) |
| 끄덕임 | 예상(×4) · 기억(×4) · 노력(×4) · cm(×4) |

**늘리지 않았다.** 목록을 보면 통계만으로는 안 된다는 것이 분명하다 — `사다`·`독`·
`활동`·`cm`에 고개를 저을 이유가 없다. 반대로 `지진`·`흔들리다`의 고개 흔들기는
부정이 아니라 **그 수어 자체의 움직임**(흔들리는 모양)일 가능성이 크고, `취소`·
`미달`은 진짜 부정 표지일 수 있다. 이 둘을 가르려면 수어를 아는 사람이 봐야 한다.

부정 표지를 잘못 붙이면 **뜻이 반대로 읽힌다.** 비수지에서 우리가 지켜 온 규칙
그대로다 — 확인 전에는 지어내지 않는다.

**9월 당사자 평가 때 물어볼 것**으로 남긴다. 위 표를 그대로 보여 주고
"이 낱말에 고개를 젓습니까/끄덕입니까"를 확인하면, 확인된 것만 목록에 넣어
`aihub_nms.py`를 다시 돌리면 된다(코드는 이미 그 구조다).

## 아바타 카메라는 그대로 둔다 — 잘리는 것은 손이 아니라 어깨였다 (2026-08-18)

사진에서 아바타의 팔이 화면 끝에 닿아 있어 "손이 잘린다"고 보고 카메라를 뒤로
물리는 수정을 했다. **되돌렸다.** 재 보니 아니었다.

측정 세 번, 답이 세 번 달랐다 — 그래서 무엇을 재는지가 중요하다.

| 잰 것 | 결과 | 왜 이 값이 나왔나 |
|---|---|---|
| 재생 중 24프레임, 무대 일부만 | 0% | 팔이 있는 구역을 잘라내고 쟀다 |
| 재생 중 90프레임, 무대 전체 | 86% | **자막·낱말 줄의 글자**가 가장자리에 닿은 것까지 셌다 |
| 재생 중 90프레임, 3D 캔버스만 | 83% | **이것도 틀렸다** — 무대 맨 위의 재생 진행 표시줄(왼쪽 끝에서 자란다)을 아바타로 셌다 |
| 사진 22장, 닿은 세로 위치까지 | — | **아래쪽(0.89~0.97)에서만 닿는다 = 어깨·몸통** |

수어를 재생 중인 화면(`폰_07_대화_수어재생`)은 좌우 여백이 12px 남는다.

> ⚠️ **이 결론은 틀렸다 — 아래 「손은 실제로 잘리고 있었다」를 볼 것.**
> "아래쪽에서 닿는다"를 어깨로 읽었는데, 그 높이는 어깨가 아니라 **내린 손**이었다.

카메라를 물렸다면 폰에서 아바타가 23% 작아졌을 것이다. 세로 구도를 고칠 때
"잘린 손보다 작은 손이 낫다"고 적었는데, **잘리지 않았으면 작을 이유가 없다.**

교훈: 화면 픽셀을 잴 때는 **무엇을 재는 영역인지**부터 못박아야 한다. 자막이
들어간 영역을 재면 아바타 이야기가 아니라 글자 이야기가 된다. 그리고 "가장자리에
닿았다"와 "잘렸다"는 다른 말이다 — 어디에 닿았는지를 함께 봐야 한다.

당사자 평가 때 "손이 잘려 보이나요?"를 직접 묻는 편이 가장 확실하다.

## 감사 5차까지 — 오역 47건 (2026-08-18 저녁)

`suspect_align.py`를 빈도 400위까지 훑어 뜻이 닿지 않는 것만 골랐다. 누적 47건.
표현률은 96.1%(창구 93.5%)로 유지되고 오역 회귀 137건도 계속 통과한다.

4·5차에서 새로 나온 것들:

| 낱말 | 잘못 붙어 있던 글로스 | 고친 것 |
|---|---|---|
| 해상 · 해역 | **km** · **크기0** | 바다2 |
| 진입 | 불가능1 (뒷말이 붙었다) | 들어가다0 |
| 충돌 | **벨트1** (안전벨트 문장과 얽혔다) | 사고1 |
| 일자 | 끓다1 | 며칠0 |
| 기울여 | 방송1 | 듣다0 |
| 상세 | **양천0**(지명) | 자세하다1 |
| 거주자 | **광산**(지명) | 주민0 |
| 통행객 · 해수면 | 대조기 | 사람1 · 바다2 |
| 좌석 | 퍼센트1 | 앉다1 |
| 국내 | 영상1(텔레비전) | 한국1 |

**지명 표시가 못 잡는 지명이 있다.** `양천0`·`광산`은 행정구역 꼬리(시·군·구·동…)로
끝나지 않고 지문자 목록(서울 지명·거리 이름 1,015종)에도 없어 표시가 안 됐다.
표시는 사람 검토를 **좁혀 주는 것이지 대신해 주지 않는다** — 목록을 넓히려면
행정구역 전체 이름표가 필요하고, 그건 공공데이터포털 키로 받을 수 있다(대피소
목록과 같은 자리).

**남은 것 대부분은 오역이 아니다.** 400위까지 보면서 고친 것은 47건이고, 나머지는
한국어와 수어가 서로 다른 낱말을 쓰는 자리였다(대피→도망, 한파→춥다, 삼가→하지마,
결빙→얼다). 도구가 "글자를 안 겹친다"로 걸러 내는 것이라 그런 자리가 많이 걸린다.


## 손은 실제로 잘리고 있었다 — 앞 절을 뒤집는다 (2026-08-18 저녁)

앞에서 "닿는 것은 어깨"라고 결론 냈는데 틀렸다. 픽셀 줄을 **구간 단위로** 다시
보고서야 알았다(받기 화면, 폰 390px):

    y=500 줄의 아바타 구간 → [141~251] 몸통 · [334~388] **오른손**
    y=520 줄               → [141~250] 몸통 · [340~352][354~367][370~384][386~389] 손가락
    화면은 389에서 끝난다

**몸통과 떨어진 별도 덩어리**다. 어깨라면 몸통에 붙어 있어야 한다. 어깨는 더 위
(y≈380~420)에 있고 거기서는 x 42~314로 여유가 있다.

내가 "아래쪽=몸통"이라고 읽은 것이 잘못이었다. 손을 내린 자세에서는 손이
화면 아래쪽에 온다. **위치만 보고 부위를 단정하면 안 된다** — 몸통과 이어져
있는지를 봐야 한다.

그래서 가로 시야 보정을 넣는다. fov는 세로 기준이라 세로로 긴 화면에서는 가로가
좁아진다. 담고 싶은 가로 폭을 1.4m로 두고 화면 비율에 맞춰 카메라를 물린다
(폰에서 거리 2.0 → 약 2.4, 아바타가 18% 작아진다). 넓은 화면에서는 그대로다.

**세로를 고칠 때 적어 둔 말이 그대로 적용된다 — 잘린 손보다 작은 손이 낫다.**
수어에서 손 모양은 뜻 그 자체라 잘리면 그 낱말은 못 읽는다.

측정에서 배운 것을 다시 적어 둔다. 같은 화면을 네 번 재면서 답이 네 번 달랐다.
영역을 잘못 잡으면 자막 글자를 아바타로 세고, 접촉 위치만 보면 손을 어깨로 읽는다.
**무엇을 재는지, 그리고 그 수치가 무엇을 뜻하는지를 매번 확인해야 한다.**

## CTC를 묻기 화면에 **그대로 갈아 끼우지 않는다** (2026-08-18)

연속 인식 `ctc-v1`이 곧 끝난다. 브라우저 쪽 길(`ctcRecognizer.ts`)과 배포
스크립트(`deploy_ctc.sh`)는 준비돼 있고, 내보내기·디코딩 대조까지 미리 확인했다.
그런데 **묻기 화면을 CTC로 바꾸면 지금 되는 것이 안 되게 된다.**

| | 고립 단어 `iso-v2` | 연속 `ctc-v1` |
|---|---|---|
| 클래스 | **13,576** | **8,147** |
| 학습 데이터 | 재난안전 + 수어영상 합본 | 재난안전만 |

의도 판정에 쓰는 낱말 47개 중 `화장실`은 iso-v1(8,147)에 없고 iso-v2부터
인식된다 — CTC로 갈아 끼우면 **"화장실 어디"를 다시 못 알아듣는다.** 오늘
`check_intent_words.mjs`로 막아 둔 바로 그 실패다.

그래서 셋 중 하나를 골라야 한다.

1. **CTC를 합본으로 다시 학습한다**(가장 깨끗하다. GPU 시간이 든다)
2. 두 경로를 함께 둔다 — 문장은 CTC, 의도 낱말은 고립 단어. 둘의 결과를 합치는
   규칙이 새로 필요하고, 그 규칙이 틀리면 원인을 찾기 어렵다
3. 묻기는 지금대로 두고 CTC는 **대화 화면의 자유 발화**에 먼저 쓴다
   (거기서는 의도 판정을 하지 않으므로 어휘가 좁아도 손해가 적다)

**지금 정하지 않는다.** ctc-v1의 최종 WER를 보고, 합본 재학습에 드는 시간을
견줘서 고른다. 다만 **아무 생각 없이 갈아 끼우면 퇴보**라는 것은 기록해 둔다 —
클래스 수가 줄어드는 교체는 언제나 이 위험이 있다.

**쓰기 전에 `deploy_ctc.sh`를 돌리지 말 것.** 오프라인 목록은 `public/models/`
아래를 재귀로 훑는다. 앱이 아직 안 쓰는 31MB 모델을 미리 올려 두면 이용자가
받는 오프라인 세트만 커진다(지금 109.8MB). 잇는 작업과 같이 하면 된다.


## 가로 보정 확인 — 그리고 자동 측정은 또 틀렸다 (2026-08-18 저녁)

카메라를 물린 뒤 같은 화면을 다시 찍어 쟀다.

    전: 무대 가로 26~389 / 폭 390 · 여백 0px   (y=500 줄에 [334~388] 오른손)
    후: 무대 가로 89~298 / 폭 390 · 여백 89px  (좌우 끝에 아바타 픽셀 없음)

태블릿은 여백 319px로 이전과 같다 — 가로가 넓어 카메라를 물리지 않는다.

**자동 측정(90프레임)은 보정 뒤에도 87%가 나왔는데, 그것도 틀렸다.** 무대 맨 위
8px에 있는 **재생 진행 표시줄**이 왼쪽 끝에서 자라는데 그것을 아바타로 셌다.
같은 사진을 손으로 뜯어 보니 좌우 끝에 닿은 것은 그 막대뿐이었다(y 115~120,
x 0~40). 스크립트에서 위 8px를 잘라냈다.

같은 질문에 다섯 번 답을 냈고 **자동 측정이 세 번 틀렸다.** 픽셀을 세는 검사는
"무엇을 세고 있는가"를 사람이 한 번은 눈으로 확인해야 한다. 이 건에서 믿을 수
있었던 것은 **사진 한 장을 구간 단위로 뜯어 본 것**뿐이었다.

## 아바타 프레이밍 — 결국 필요했던 것은 가로 보정 하나 (2026-08-18 마무리)

하루 동안 이 한 가지를 여섯 번 재고 세 번 판단을 뒤집었다. 정리해 둔다.

| 변경 | 판정 | 왜 |
|---|---|---|
| 가로 시야 확대(WIDE 1.4m) | **필요했다** | 오른손이 화면 끝에 잘리고 있었다(몸통과 떨어진 덩어리로 확인) |
| OrbitControls target 동기화 | **필요했다** | 이것이 없으면 세로 조정이 통째로 무효 |
| 세로 경계 1.91 → 1.6 | **되돌렸다** | 고장 난 프레이밍에서 잰 값으로 내린 판단이었다 |

**빌드에 값이 들어 있어도 화면은 안 바뀔 수 있다.** `OrbitControls`가 매 프레임
시선을 자기 `target`으로 되돌리기 때문에, `camera.lookAt`만 부르면 무효다.
`makeDefault`를 주고 `controls.target`을 함께 옮겨야 한다. 카메라를 **뒤로 물리는**
변경(거리)은 target과 무관해서 먹혔고, 그래서 "가로는 되는데 세로는 안 되는"
헷갈리는 상태가 됐다.

**고장 난 상태에서 잰 값으로 기준을 바꾸면 안 된다.** "머리 위가 187px 비었다"는
관찰은 사실이었지만, 그 빈 공간은 프레이밍이 틀어져서 생긴 것이었다. 원인을 고친
뒤 다시 재니 예전 값(1.91)이 맞았다.

최종(폰 390px): 좌우 여백 78px · 머리 위 55px · 아바타 세로 180~559.
아침에는 좌우 여백 0px이었다(오른손이 잘림).

## 2026-08-19 — g2ts-v1(글로스→한국어)을 **배포하지 않는다**

학습은 끝났고 수치도 좋다. 재난문자 검증 400문장에서 음절 BLEU-4 **69.7** ·
음절 F1 **84.0**(정밀도 85.0 · 재현율 83.0). 사람 번역과 글자까지 똑같이 나온
문장도 있었다.

    글로스: 흐르다1 비내리다1# 차오르다1 경보1 강1 지면1 넘치다1# 피해1 …
    사람  : 낙동강 홍수경보 발령, 하천범람 등 피해우려, 피해예방에 유의하시기 바랍니다.
    모델  : 낙동강 홍수경보 발령, 하천범람 등 피해우려, 피해예방에 유의하시기 바랍니다.

**그런데 앱에 쓸 자리가 없다.** 이 모델을 쓸 곳은 창구 대화에서 농인의 답을
**소리로 내보내는** 자리인데, 거기 오는 글로스는 전부 일상어다. 넣어 보고 쟀다:

    머리 아프다        → 오늘 아침 기온이 크게 오르고 있습니다.
    약 먹다 아니다      → 오늘 아침 기온이 크게 오르고 있습니다.
    화장실 어디        → 오늘 아침 기온이 크게 오르고 있습니다.
    예약 하다 원하다    → 이번 비는 눈이 내렸다 그치고 파고 있습니다.

t2gs와 같은 병이다 — 재난 말뭉치로만 배워서 **그 밖에서는 자신 있게 틀린다.**
다만 t2gs보다 나쁘다. t2gs가 틀리면 아바타가 이상한 수어를 하고 농인이 갸웃한다.
g2ts가 틀리면 **병원 직원이 "머리 아프다"를 "오늘 아침 기온이…"로 듣는다.**
농인은 자기 말이 어떻게 전달됐는지 볼 수 없다. 되돌릴 방법이 없는 오류다.

t2gs처럼 domain으로 가를 수도 없다. t2gs는 재난문자 본문이라는 **쓸 자리가
있었지만**, 글로스→한국어가 필요한 자리는 창구 대화뿐이고 그곳은 전부 일상어다.

그래서 **규칙 기반 `glossToKorean.ts`를 그대로 둔다.** 뻣뻣하지만 틀리지 않는다.
체크포인트는 `~/sbruns/g2ts-v1`에 남겨 둔다 — 일상 병렬 데이터(수어영상 SEN
문장 목록)를 구하면 그때 다시 학습해서 쓴다. 그것이 이 모델이 쓸모 있어지는
유일한 조건이다.

측정: `python -m ml.eval_t2g --checkpoint ~/sbruns/g2ts-v1 --data ~/sbdata/script
--direction gloss2text --limit 400` (이번에 `--direction`을 추가했다)

## 2026-08-19 — ctc-v1(연속 문장 인식)은 학습을 끝냈지만 **묻기에 아직 잇지 않는다**

검증 WER **0.2176**(에폭 59 · 학습 135,446 / 검증 6,617클립 · 7.43M).
이어서 수어해도 낱말열로 읽힌다는 뜻이다. 지금 묻기 화면은 낱말을 하나씩
끊어 쌓아야 해서 농인에게는 대화가 아니라 조작이다. 이것을 풀 모델이다.

**그런데 그대로 갈아 끼우면 안 된다.** CTC는 클래스가 8,147종이고 배포된
iso-v2는 13,576종이라 **집합이 다르다.** 의도 판정 낱말 47개를 CTC 목록에
맞춰 보니 **39/47만 있다.**

    ✗ 장소:hospital  구급차 의사 간호
    ✗ 장소:toilet    화장실 소변      ← 둘 다 없다
    ✗ 무슨 일        일어나다
    ✗ 재난 낱말      홍수
    ✗ 도움           구조

`화장실`과 `소변`이 **둘 다** 없어서, CTC로 바꾸는 순간 **화장실 의도가 통째로
죽는다.** 오류도 안 나고 경고도 없다 — 농인이 화장실을 물으면 "못 알아들었어요"만
나온다. 이 앱에서 가장 찾기 어려운 실패의 전형이다(`check_intent_words.mjs`가
iso-v2에 대해 잡아내던 것과 같은 병이다).

갈 수 있는 길은 셋이다.
1. CTC로 교체 → **화장실 의도 사망.** 안 된다.
2. iso-v2 낱말 쌓기 + CTC를 **함께** 돌려 후보를 합친다 → 의도는 안 죽고 연속
   수어도 읽힌다. 다만 브라우저에서 추론이 두 배가 되고 20MB를 더 받아야 한다.
3. 지금은 두지 않는다. ← **이것을 고른다**

3을 고른 이유: 2가 실제로 나은지는 **농인이 이어서 수어하는 것을 받아 봐야**
알 수 있는데, 그 측정을 아직 못 했다. 시연을 앞두고 재보지 않은 것을 넣지 않는다.
체크포인트·배포 스크립트(`ml/jobs/deploy_ctc.sh`)·브라우저 디코더
(`src/recognition/ctcRecognizer.ts`)·일치 검사(`ml.tools.ctc_parity`)는 전부
준비돼 있으니, 9월 당사자 평가에서 이어서 수어한 표본을 받으면 2안을 재고 정한다.

숫자는 사이트에 **"학습은 끝났고 앱에는 아직 잇지 않았다"**로 적는다.

## 2026-08-19 — 웹 배포용 "가벼운 판"을 만들지 않는다

업로드를 줄이려고 서비스워커의 필수(407)+고빈도(957) 목록에 든 글로스 1,301종만
남기고 11,529개를 뺀 93MB 판을 만들었다. **실제로 돌려 보고 지웠다.**

    묻기 → 화면: "북서쪽 350미터 대피하세요"      ← 멀쩡해 보인다
           아바타 프레임 1 · 글로스 0             ← 수어가 안 나온다
           없는 파일 요청 21건(발생0·요즘1·새0·방역0·공…)

필수·고빈도 목록은 **미리 받아 둘 것**을 고른 목록이지 *그것만 있으면 되는*
목록이 아니다. 나머지는 필요할 때 서버에서 가져오고, 없으면 조용히 건너뛴다 —
오류도 경고도 없고 한국어 자막은 그대로 뜬다. 이 저장소가 `check_silent_skip.mjs`
까지 만들어 막고 있는 바로 그 실패를 **배포 형식으로 다시 만들 뻔했다.**

업로드 74MB를 아끼는 대가로 시연 도중 수어가 안 나오는 것은 남는 장사가 아니다.
`deploy/사이트zip만들기.sh`는 이제 전체 판 하나만 만든다.

**교훈:** 용량을 줄이는 최적화는 **줄인 뒤 실제로 눌러 보기 전까지 끝난 게 아니다.**
이 앱에서 빠진 데이터는 오류가 아니라 침묵으로 나타난다.

## 2026-08-19 — 글로스 파일명에서 `:`를 없앴다 (윈도우에서 압축이 안 풀렸다)

`safe_name`이 `:`를 **일부러 남기고** 있었다 — `re.sub(r"[^\w가-힣:#]+", "_", text)`.
그래서 `날짜:10월10일.json` · `시:8시26분.json` 같은 이름이 1,365개 있었다.

리눅스·웹에서는 아무 문제가 없다. 자동 검사 266개도 전부 통과했다. 그런데
**윈도우는 파일명에 `:`를 못 쓴다.** 사용자가 노트북용 zip을 풀다가 멈췄다:

    오류 0x80070057: 매개 변수가 틀립니다.
    시:8시26분.json

건너뛰면 압축은 풀리지만 **날짜·시각 수어 1,365종이 통째로 빠진다.** 재난문자에
가장 자주 나오는 것들이고("오늘 10월 10일 8시 26분…"), 빠져도 오류가 안 난다 —
아바타가 조용히 설 뿐이다. 이 저장소가 계속 잡아 온 그 실패가 **배포 형식**에서
다시 나타난 것이다.

고친 것:
- `safe_name`을 `[^\w가-힣]+ → _` 로. `#`도 뺐다(URL 조각 구분자라 fetch가 깨진다)
- `ml/tools/fix_gloss_filenames.py` — 이미 만들어진 파일과 `bank.json`을 함께 옮긴다.
  앱은 `bank.json`의 `file`을 그대로 fetch하므로 다른 코드는 손댈 것이 없었다.
  이름 충돌 0건을 확인하고 적용했다(겹치면 동작이 덮어써진다).
- `scripts/check_filenames.mjs` — 윈도우 금지 문자·예약 이름(CON·PRN…)·끝의 점/공백,
  그리고 `bank.json`의 `file` 값까지 본다. `rebuild_data.sh`에 넣었다.

확인: 시각·날짜 조각 1,365종 재생 가능 · 브라우저에서 날짜 낱말 21프레임 재생 ·
4xx/5xx 0건 · 검사 25,923개 이름 전부 통과.

**교훈:** 개발 환경(리눅스·웹)에서 되는 것과 **사용자가 받는 형식**(윈도우 zip)에서
되는 것은 다르다. 배포 꾸러미는 만들고 나서 **받는 쪽 환경 기준으로** 한 번 봐야 한다.

## 2026-08-19 — 노트북용 `.bat`이 윈도우에서 통째로 깨졌다 (UTF-8 + `chcp`)

촬영하려고 zip을 받아 `시작하기_윈도우.bat`을 눌렀더니 서버가 아예 안 떴다:

    '(騷??'은(는) 내부 또는 외부 명령, 실행할 수 있는 프로그램, 또는
    배치 파일이 아닙니다.        ← 한글 줄이 전부 이렇게 나왔다

`.bat`을 UTF-8로 저장하고 2번째 줄에 `chcp 65001`을 둔 것이 원인이다. cmd.exe는
`.bat`을 **ANSI 코드페이지**(한국어 윈도우면 CP949)로 읽는다. 게다가 **코드페이지를
파일 중간에서 바꾸면 cmd가 파일을 읽던 바이트 위치를 어긋나게 잡아** 그 뒤 줄이
통째로 망가진다. 그래서 한글이 든 줄이 *명령으로 해석되어* 오류가 쏟아졌다.
리눅스에서는 `bash -n`도 통과하고 아무 문제가 없다 — 받는 쪽에서만 죽는다.

고친 것:
- `.bat`에서 `chcp`를 없애고, 꾸리면서 **CP949 + CRLF**로 변환해 넣는다
- `서버.ps1`은 **UTF-8 BOM**을 붙여 넣는다 (Windows PowerShell 5.1은 BOM이 없으면 ANSI로 읽는다)
- `시작하기_맥.command`는 UTF-8 그대로 (맥은 UTF-8이 기본)
- `make_laptop_zip.sh`가 변환 뒤 **되돌려 읽어 확인**하고, `chcp`가 남아 있으면 멈춘다

받는 쪽에서 확인: 원본 zip을 푼 폴더에 이번 수정본을 덮어쓴 12,988개로 브라우저
실구동 — 소개 페이지·앱 전환·아바타 622프레임·4xx/5xx 0건.

**교훈:** 2026-08-19의 파일명 `:` 사고와 **같은 병이다.** 배포 꾸러미는 만든 환경이
아니라 **받는 쪽 환경의 인코딩·파일명 규칙**으로 한 번 봐야 한다. 이번에는 자동
검사가 없어서 사용자가 발표 전날 밤에 발견했다.

## 2026-08-19 — 앱 아바타를 `real-avaturn`으로 되돌린다 (손이 뭉개졌다)

요청을 받아 당사자 화면 아바타를 `real-david.glb`(실사 인물 남2)로 바꿨다가
**되돌렸다.** 손가락이 뭉개져 주걱처럼 보였다. 정지 자세에서도 그랬다.

원인은 **리깅이 다른 것**이다. 손가락 뼈 수를 세어 확인했다:

    real-avaturn     30개 = 손가락당 3마디 (Thumb1·2·3)
    real-david       40개 = 손가락당 4마디 (Thumb1·2·3·4)
    real-avatarsdk   40개 = 4마디  + 옷에 제작사 로고(발표용 부적합)

`glbRetarget.ts`의 `HAND_SEGS`는 **3마디 기준**이다. `childOf`도 `1→2`, `2→3`까지만
잇고, 마지막 마디는 `make(...3)`에 자식 이름을 넘기지 않는다. 3마디 리그에서는
자식이 없어 기본축 `(0,1,0)`으로 떨어지지만, 4마디 리그에서는 `Thumb4`가 자식으로
잡혀 **기준축이 달라진다.** 게다가 끝마디(`Thumb4`)는 아예 구동되지 않는다.

**교훈: 아바타는 얼굴로 고르는 것이 아니다.** 수어는 손이 전부라, 리깅이 다른
모델로 바꾸면 리타게팅을 먼저 그 리깅에 맞춰야 한다. 바꾸기 전에 **정지 자세의
손 모양부터** 눈으로 볼 것 — 재생 중 스크린샷은 재생을 멈추므로 정지 컷이 오히려
비교에 낫다.

남성 아바타를 쓰려면 `HAND_SEGS`와 `childOf`를 4마디까지 다루도록 고치고, 끝마디도
구동한 뒤 다시 비교해야 한다. 지금은 검증된 판을 쓴다.

`avatars.ts`의 `APP_MODEL_URL` 주석에 같은 내용을 남겼다.
