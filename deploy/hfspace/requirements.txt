# 배포용(Render·HF Spaces 등) — 이 워크스페이스와 달리 torch가 없는 환경 전제.
#
# **CPU 전용 torch를 명시적으로 받는다.** 기본 인덱스의 torch는 CUDA 런타임까지 끌고 와
# 2GB가 넘고, 무료 호스팅의 빌드 용량·시간을 넘긴다.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.5.1+cpu

transformers==4.44.2
sentencepiece
numpy<2
fastapi
uvicorn[standard]
