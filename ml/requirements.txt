# SignBridge 학습 파이프라인 의존성.
#
# KOREN AI Cloud의 `torch2.5.1 py3.12 cuda12.4` 가상환경을 전제로 하며,
# torch/torchvision은 이미 설치돼 있다고 본다(직접 설치하면 CUDA 버전이 어긋날 수 있다).
#
#     pip install -r ml/requirements.txt
#
# torch 버전 호환: 학습·내보내기 전 과정을 **torch 2.5.1과 2.13 양쪽에서 실행 검증**했다.
# 2.5.1에서 걸렸던 두 가지는 코드 쪽에서 이미 해결돼 있다.
#   · dynamo ONNX 내보내기가 BatchNorm1d 변환에 실패 → 프런트엔드를 LayerNorm으로 변경
#     (가변 길이 패딩 배치에서 BatchNorm이 통계를 오염시키는 문제도 함께 해결)
#   · `Dim.DYNAMIC`이 2.6부터라 2.5.x에 없음 → `Dim.AUTO`로 자동 대체
# 참고: 2.5.1에서는 int8 양자화 압축률이 낮다(1.2× vs 2.13의 3.2×). fp32(약 4MB)를 써도 무방.

numpy>=1.24

# ── ETL ────────────────────────────────────────────────────────────────
# MediaPipe 랜드마크 재추출(권장 경로)에 필요. CPU 워크스페이스에만 있으면 된다.
mediapipe>=0.10.14
opencv-python-headless>=4.9      # GUI 없는 서버 환경이므로 headless

# ── 내보내기 ────────────────────────────────────────────────────────────
onnx>=1.16
onnxruntime>=1.18                # 내보낸 그래프 검증 + int8 동적 양자화
onnxscript>=0.1                  # torch.export 기반 ONNX 내보내기에 필요

# ── 언어 단계(글로스 ↔ 한국어) ────────────────────────────────────────
transformers>=4.40
sentencepiece                    # KoBART 토크나이저

# 참고: 특징 일치 검증(ml/tools/feature_parity.py)은 파이썬 패키지가 아니라
# Node 22+ 를 쓴다(타입 스트리핑으로 landmarks.ts를 직접 실행).
