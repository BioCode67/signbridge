# 터미널 나눠 쓰기 — 작업 공간 셋

VSCode에서 터미널을 여러 개 열어 **동시에 다른 일**을 하려고 만든 구성이다.

## 왜 폴더를 나누나

한 폴더에서 두 세션이 동시에 일하면 서로를 깨뜨린다. 특히:

- `npm run build`가 `dist/`를 통째로 다시 쓴다 → **다른 세션의 e2e가 404로 실패**한다
  (해시 파일명이 바뀌어 `assets/TalkMode-Dm0VZMwg.js`를 못 찾는다. 2026-08-20에 세 번 겪었다)
- `git add -A` 가 남의 편집까지 커밋한다
- `public/data/`를 다시 만드는 동안 상대는 반쪽 사전을 읽는다

그래서 **git worktree**로 완전히 분리했다. 같은 저장소, 다른 폴더, 다른 가지.

```
/home/jovyan/app          main 작업 (지금까지 쓰던 곳)  · claude/sign-language-...
/home/jovyan/work/sign    수어 동작 구현               · work/sign
/home/jovyan/work/avatar  아바타 만들기 · 질의응답      · work/avatar
/home/jovyan/work/ui      웹사이트 UI                  · work/ui
```

`node_modules`는 심볼릭 링크로 공유한다(854MB × 3을 아낀다). 그래서 각 폴더에서
`npm install`을 다시 할 필요가 없다. **패키지를 새로 깔면 `app`에서 깔 것.**

## 터미널 여는 법 (VSCode)

1. `Ctrl` + `` ` `` (백틱) — 터미널 패널 열기
2. 오른쪽 위 **`+`** 를 눌러 터미널 추가. 또는 `Ctrl` + `Shift` + `` ` ``
3. 터미널 목록에서 **분할 아이콘**(`⑃`)을 누르면 좌우로 나란히 볼 수 있다
4. 각 터미널에서 자기 폴더로 이동한 뒤 Claude Code를 시작한다

```bash
cd ~/work/sign     && claude      # 터미널 1 — 수어 동작
cd ~/work/avatar   && claude      # 터미널 2 — 아바타
cd ~/work/ui       && claude      # 터미널 3 — UI
```

> 터미널 이름을 바꿔 두면 헷갈리지 않는다 — 터미널 탭을 더블클릭해서 `수어`·`아바타`·`UI`.

## 지켜야 할 것 셋

**① 자기 폴더 밖을 고치지 않는다.**
`~/work/ui`에서 `ml/`을 고치면 나중에 합칠 때 충돌한다. 남의 영역이 필요하면
그 터미널에 부탁하거나, 합친 뒤에 한다.

**② e2e는 한 번에 하나만 돌린다.**
브라우저 검사는 CPU를 많이 쓴다. 두 개가 겹치면 **둘 다 엉뚱하게 실패**한다
(실측: 단독으로 돌리면 통과하는데 겹치면 묻기 검사 네 개가 함께 실패했다).
돌리기 전에 물어보고 돌릴 것.

**③ `public/data/` 재생성은 `app`에서만.**
동작 사전·번역 사전은 서로 물려 있어(동작 사전 → 번역 사전 → 어순표) 반쯤
만들어진 상태를 남에게 보이면 안 된다. `bash ml/jobs/rebuild_data.sh` 는 `app` 전용.

## 합치기

각 가지에서 일이 끝나면 `app`에서 받는다.

```bash
cd /home/jovyan/app
git merge work/ui        # 충돌이 나면 그 자리에서 고른다
npm run build && python3 -u scripts/e2e_app.py     # 합친 뒤 반드시
```

**합친 뒤에는 e2e를 꼭 돌린다.** 각자 자기 영역만 봤으니, 맞물리는 자리는
합쳐 봐야 안다.

---

# 각 터미널이 맡을 일

## 터미널 1 — 수어 동작 (`~/work/sign`)

**목표:** 아바타 손동작을 실제 수어로 읽히게 만든다.

**지금 상태**
- 손 깊이(z)를 원본에서 되살려 실었다(수록률 96%). 뼈 길이 변동 0.182 → 0.142
- 손가락 굽힘은 **손가락당 하나**를 줄자 비율로 재고 해부학 비로 나눈다
- 뒤틀림은 사라졌으나 **손모양의 종류**까지는 아직 못 살린다

**남은 것**
1. 손모양 정확도 — `삼`처럼 특정 손가락만 펴는 모양이 구분되는가
2. 팔 3D — 지금은 2D. 3D로 바꾸면 조각이 섞일 때 튄다는 기록이 있는데(`DECISIONS.md`)
   **손처럼 조각 안에서만 쓰면** 괜찮을지 재 볼 것
3. 이음매(`BLEND_FRAMES = 6`, 200ms)가 실제 수어자 전환과 맞는지
4. 비수지 — 고개 끄덕임·흔들기는 붙어 있다. 마우징(입 모양)은 아직

**주로 보는 파일**
```
src/sections/sign/glbRetarget.ts   손가락·팔 리타게팅
src/sections/sign/composeLocal.ts  조각 잇기·보간
src/sections/sign/Avatar3D.tsx     카메라·표정
ml/etl/export_web_bank.py          웹으로 무엇을 싣는가
```

**확인 방법**
```bash
node scripts/check_filenames.mjs
node --experimental-strip-types --import ./scripts/ts-register.mjs scripts/check_silent_skip.mjs
# 손 깊이가 문장까지 도달했는가 — data-sign-handz 가 1이어야 한다
```

---

## 터미널 2 — 아바타 만들기 · 질의응답 (`~/work/avatar`)

**목표:** 더 나은 아바타를 고르거나 만든다.

**반드시 알아야 할 것**
아바타는 **얼굴로 고르면 안 된다.** 수어는 손이 전부다.

```
real-avaturn     손가락 뼈 30개 = 손가락당 3마디   ← 지금 쓰는 것
real-david       손가락 뼈 40개 = 4마디            ← 손이 주걱처럼 뭉갠다
real-avatarsdk   손가락 뼈 40개 = 4마디 + 옷에 제작사 로고
```

`glbRetarget.ts`의 `HAND_SEGS`가 **3마디 기준**이라 4마디 리그에서 마지막 마디의
기준축이 달라지고 끝마디가 아예 안 움직인다. 새 아바타를 쓰려면 **리타게팅을
4마디까지 다루도록 먼저 고치고**, 정지 자세의 손 모양부터 눈으로 볼 것.

**할 수 있는 일**
1. `HAND_SEGS`·`childOf`를 4마디까지 다루도록 확장 → 남성 아바타 선택지가 열린다
2. 무료 아바타를 받아 손 리깅을 확인(RPM·Avaturn·Mixamo·VRoid)
3. 옷·머리 메시가 손목에서 뚫려 보이는 문제
4. ARKit 블렌드셰이프로 마우징(입 모양) — 원본에 마우징 225종이 있다

**뼈 구조 확인법**
```bash
python3 - <<'PY'
import json, struct, pathlib
d = pathlib.Path("public/models/이름.glb").read_bytes()
ln = struct.unpack('<I', d[12:16])[0]
j = json.loads(d[20:20+ln])
names = [n.get('name','') for n in j['nodes']]
fing = [n for n in names if any(k in n for k in ("Thumb","Index","Middle","Ring","Pinky"))]
print(f"손가락 뼈 {len(fing)}개 →", "3마디" if len(fing)==30 else "4마디" if len(fing)==40 else "확인 필요")
PY
```

---

## 터미널 3 — 웹사이트 UI (`~/work/ui`)

**목표:** 보기 좋고 안 깨지게.

**지금 상태**
- 소개 페이지 섹션 10개 · 당사자 화면 탭 4개
- 폰·태블릿(가로/세로)·키오스크·오프라인 자동 검사 통과
- 소개 페이지 검사(`scripts/check_landing.py`) 15개 통과

**할 수 있는 일**
1. 데스크톱(1920×1080)에서 양옆이 비어 보이는 문제 — 최대 폭을 두거나 배치를 바꾼다
2. 키오스크 화면 다듬기
3. 색·글꼴·여백 정리
4. 접근성 — 대비, 초점 표시, 화면 낭독기

**절대 깨뜨리면 안 되는 것**
- **화면에 적힌 숫자는 원본과 같아야 한다.** 고치면 검사가 잡는다:
  `node --experimental-strip-types --import ./scripts/ts-register.mjs scripts/check_site_numbers.mjs`
- **버튼 높이 36px 이상** — e2e가 잰다(농인 사용자가 실제로 누른다)
- **가로 스크롤 없음** — 모든 화면 크기에서
- 계측점(`data-sign-*`)을 지우지 말 것. 화면만 봐서는 못 잡는 실패를 그것으로 잡는다

**확인 방법**
```bash
npm run build
python3 scripts/check_landing.py          # 소개 페이지
python3 -u scripts/e2e_app.py             # 앱 (다른 터미널과 겹치지 말 것)
```

---

## 공통 규칙

- 커밋 author는 **BioCode67 &lt;6wngud@gmail.com&gt;**, AI 서명·Co-Authored-By 없음
- 주석과 문서는 한국어
- **측정한 것만 말한다.** 추정치를 수치처럼 적지 않는다
- 판단이 필요한 결정은 `DECISIONS.md` 맨 아래에 근거와 함께
- 각 폴더에도 `CLAUDE.md`가 있다 — Claude Code가 자동으로 읽는다
